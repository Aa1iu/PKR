# 一、这个项目是否适合作为课程最终项目？



## 直接结论：非常适合，而且比原方案更好。



你的课程四个模块的核心内容是：CNN 与深度学习基础 → YOLO 目标检测 → 大语言模型与 Transformer → RAG 与 Agent。你这个项目\*\*直接命中模块三和模块四的全部核心知识点\*\*：



| 你项目中的技术点                  | 对应课程内容                            |
| --------------------------------- | --------------------------------------- |
| 知识图谱构建（LLM 提取概念+关系） | 模块三：预训练语言模型、LLM API 调用    |
| RAG 检索增强                      | 模块四：RAG 原理与模块                  |
| 文档解析 → Embedding → 向量检索   | 模块三：分词与词向量；模块四：LangChain |
| AI 聊天助手                       | 模块四：知识问答助手搭建与分发          |
| 可选：用 LangChain 编排 RAG 流程  | 模块四：基于 LangChain 的知识问答助手   |



**相比原方案的优势**：原方案（PPT 讲解）的核心工作量在前端渲染和高亮坐标系统，这些不涉及课程内容。新方案（知识库管理 + 知识图谱 + RAG 对话）的核心工作量在 LLM 调用、Embedding、向量检索、图谱构建——**全部是课程内容的直接实践**。你的课程报告有东西可写，演示视频有技术亮点可展示。



**工作量评估**：一个人完成全部功能约需 40-60 小时（含学习时间）。如果是一个小组（2-3 人），分工合理的话 3-4 周可以完成。工作量对于课程大作业来说是饱满的。



**但需要注意**：这个项目的前端工作量不小（知识图谱可视化、文档列表管理、AI 对话界面），不要把全部时间花在前端而忽略了课程核心技术（RAG、LLM）。建议组内分工时至少有一人专注后端 RAG 管线。



---



# 二、功能完善建议



以下是我基于你的思路，结合课程内容，完善后的完整功能设计。



---



## 模块一：知识库管理



### 1.1 创建与管理知识库



用户可以创建多个知识库，每个知识库有名称、描述和主题标签（如"深度学习"、"计算机组成原理"）。知识库列表作为软件首页。支持重命名、删除（需二次确认，级联删除所有关联数据）、导出知识库结构（导出为 JSON，含概念列表和关系列表）。



### 1.2 文档导入



支持向知识库中添加多种格式的文档：PPT（.pptx）、PDF、Word（.docx）、Markdown、纯文本。导入方式支持拖拽上传和文件选择器批量导入。



导入后自动完成三件事。第一，文档解析：提取文本内容、页码/段落结构、基本元信息（文件名、大小、页数、导入时间）。第二，文档分块：将长文档按语义段落切分为合适长度的文本块（每块 300-800 字），用于后续的向量化和 RAG 检索。第三，入库并触发异步分析：文档存入数据库后，后台启动 AI 分析任务（概念提取 + 关系识别）。



### 1.3 文档管理



知识库内的文档以列表或卡片形式展示，显示文件名、类型图标、页数、导入时间、处理状态。支持全文搜索（在知识库内跨文档搜索关键词）、删除、重命名。



---



## 模块二：知识图谱



### 2.1 概念提取（AI 核心功能）



系统对知识库内所有已解析文档的文本进行 AI 分析，提取核心概念列表。每个概念是一个知识库级别的实体，不对应单一文档。



每个概念节点包含：概念名称、简洁定义（AI 根据文档内容生成或从文档中提取）、概念类型（基础概念 / 技术方法 / 工具框架 / 应用场景 / 其他——用于图谱着色）、关联文档列表（该概念在哪些文档中被提及，含具体页码/段落位置）、首次出现位置。



增量更新策略：用户添加新文档后，只对新文档做增量分析——提取新概念并与已有概念做去重合并（名称相似度 > 阈值的合并）。新概念与已有概念之间可能存在新关系，触发增量关系识别。



### 2.2 关系识别（AI 核心功能）



系统识别概念之间的知识关联关系。关系类型包括：前置依赖（学 A 前应先学 B）、概念延伸（B 是 A 的深化或扩展）、对比关系（A 与 B 是同类不同方案）、包含关系（A 是 B 的组成部分）、应用关系（A 是 B 的应用场景）。



关系带有方向性（从 A 到 B），存储时保留方向信息。关系附带简洁说明（AI 生成的 50 字以内描述）。



### 2.3 知识图谱可视化



知识库详情页以可视化知识图谱作为核心展示方式。图谱以概念为节点、以关系为边，力导向图（force-directed graph）布局展示整个知识库的知识结构。



节点按概念类型着色（5 种类型 5 种颜色），节点大小反映关联密度（被关联越多的节点越大），边的线型和颜色反映关系类型（前置依赖用实线箭头，对比关系用虚线）。



图谱交互功能。拖拽节点可调整位置；缩放图谱整体视图（滚轮或按钮）；点击节点选中并高亮其直接关联节点；鼠标悬停节点时，弹出浮窗显示该概念的详细信息——概念名称、定义、关联文档名称列表（每个文档名可点击）。



### 2.4 节点浮窗 → 文档打开



浮窗中显示"关联文档"列表，列出所有提到该概念的文档名称。点击文档名称后，打开该文档的阅读视图。



对于 PPT 和 PDF：渲染文档内容（PPT 用截图方案、PDF 用文本分页渲染），自动跳转到提及该概念的具体页码。



对于 Markdown 和文本：直接在页面内渲染内容，自动定位到提及该概念的段落。



这个跳转链路是知识图谱与文档阅读之间的核心交互。用户的学习路径是：看图谱 → 发现感兴趣的概念 → 点浮窗看关联文档 → 打开文档阅读 → 在文档内使用 AI 对话提问。



---


## 模块三：AI 对话助手



这是一个嵌入在全平台内的浮动 AI 对话窗口，不同页面下有不同的上下文行为。



### 3.1 文档内对话（有明确上下文）



当用户在某个具体文档的阅读页面内打开 AI 对话框提问时，AI 的行为模式是"文档导师"。



AI 的 prompt 注入以下上下文：用户当前打开的文档名称和类型、当前查看的页码/段落位置、文档中当前页附近的内容（前后各 2 段，约 800 字）、RAG 检索结果（从该文档所属知识库的向量数据库中检索与问题最相关的 3 个内容片段，每个片段带来源标注）。



AI 的回答策略：优先基于当前文档内容回答、如果当前文档覆盖不够则引用同知识库中其他文档的相关内容、回答中附带引用来源（"根据《XXX 文档》第 X 页..."）、如果知识库内容确实无法回答则告知用户并建议补充相关文档。



这与课程讲过的 RAG 模式完全一致：用户问题 → Embedding → 向量检索 → 拼接 prompt → LLM 生成 → 返回带来源的回答。



### 3.2 知识库主页对话（全局上下文）



当用户在知识库主页（图谱页）打开 AI 对话框提问时，AI 的行为模式是"知识库全局顾问"。



AI 的 prompt 注入以下上下文：知识库的名称和主题描述、知识库的整体概念列表（从图谱中获取）、RAG 检索结果（从该知识库的向量数据库中检索与问题最相关的 5 个内容片段，覆盖多个文档）。



AI 的回答策略：帮助用户了解知识库的整体内容结构、回答跨文档的综合性问题（如"这个知识库中卷积相关的知识有哪些"）、如果用户的问题指向某篇具体文档，AI 在回答中主动提示"建议你打开《XXX》文档查看更多细节"、回答中附带来源标注。



### 3.3 全局对话（非知识库页面）



当用户在首页（知识库列表页）打开 AI 对话框提问时，AI 的行为模式是"学习导航助手"。



AI 的上下文相对轻量：所有知识库的名称和主题描述列表。AI 的回答策略：帮助用户选择合适的学习路径、建议用户先从哪个知识库开始、引导用户进入具体的知识库以获取更深入的回答。因为没有具体的文档上下文，AI 主要起到导航和推荐作用，不尝试做深入的知识解答。



### 3.4 通用 AI 能力（三个场景共用）



模糊问题澄清：用户问题模糊时，AI 优先用自然语言帮助收束问题（不是直接回答也不是机械反问）。



推荐提问：AI 在每次回答后给出 1-2 个推荐追问，引导用户深入探索当前话题。在文档内，推荐问题围绕当前文档内容；在知识库主页，推荐问题围绕知识库的整体知识结构。



对话历史：在同一知识库的会话内保留最近 10 轮对话历史（存储在浏览器 localStorage 或后端数据库中），不同知识库之间的对话历史互相隔离。



---



## 模块四：RAG 技术实现



### 4.1 RAG 的定位



RAG 不是单独面向用户的功能，而是支撑 AI 对话助手的核心基础设施。用户看不到"RAG 开关"，但能感受到 AI 回答是否精准引用了知识库中的文档内容。



### 4.2 RAG 数据流



第一步，文档入库时：文档文本 → 语义分块（按段落 + 语义边界切分，每块 300-800 字）→ 调用 Embedding API 生成向量 → 存入向量数据库（ChromaDB 或 FAISS），同时保留块的文本内容 + 来源元信息（文档名、页码、段落号）。



第二步，用户提问时：用户问题 → 调用 Embedding API 生成问题向量 → 在向量数据库中做语义检索（top-5 最相关块）→ 将检索到的块文本和来源信息注入 LLM 的 prompt → LLM 综合生成回答。



第三步，文档删除时：向量数据库中对应的所有块同步删除，保证检索不到已删除文档的内容。



### 4.3 Embedding 与向量数据库选型



Embedding 模型：推荐使用 API 服务（如 DeepSeek Embedding API 或 OpenAI Embedding API），省去本地部署 embedding 模型的麻烦，且中英双语效果有保障。如果课程要求展示"本地模型部署"，可以用 BGE-M3 的本地部署方案（更贴合课程模块三的 Qwen 本地推理内容）。



向量数据库：推荐 ChromaDB。pip install 即可，零配置，支持持久化存储，支持元数据过滤（可以限定只在某个知识库内检索）。FAISS 性能更好但需要手动管理持久化和元数据索引，开发量更大。



### 4.4 RAG 质量的几个关键点



分块策略不能一刀切。PPT 文档的文本是碎片化的（每页多个文本框），需要以页为单位聚合后再按语义分块。PDF 的文本是连续的，需要按段落 + 章节边界分块。Markdown 按标题层级分块。分块质量直接决定检索质量——块太大，检索到的不精准；块太小，语义不完整。



检索去重。同一个概念可能在多篇文档中出现，检索到的 top-5 结果可能有三条在讲同一件事。需要在返回前做去重（按文本相似度 > 80% 去重），保留最相关的一条。



来源标注。每条检索结果在 prompt 中带上来源信息（文档名 + 页码），AI 在回答中引用时自然带出（"根据《CNN 入门》第 3 页..."）。这让用户知道答案来自哪里，也方便用户去原文查看。



---



## 模块五：文档阅读器



### 5.1 通用文档阅读



对于所有类型的文档，提供基础的阅读视图。PPT 文档用截图方案展示（后端 LibreOffice 转为 PNG，前端用图片 + 热区显示，但首版不需要热区交互——纯图片展示即可）。PDF 文档用 Canvas 或 embed 标签渲染，或解析为文本后分页展示（用 pdf.js 或后端 PyMuPDF 提取文本）。Markdown 文档用 react-markdown 直接渲染。文本文件直接展示。



### 5.2 文档内搜索



在文档阅读页面提供 Ctrl+F 式的文档内关键词搜索（高亮匹配文字，上下翻跳）。这个功能不依赖 AI，纯前端文本匹配。



### 5.3 从图谱跳转到具体位置



从知识图谱浮窗点击文档名称后，阅读器需要自动定位到提及该概念的具体页面或段落。后端需要维护每个概念在每篇文档中的出现位置列表（页码/段落号），前端根据位置信息自动滚动到对应位置。



---



## 完整需求清单



共计 16 项功能需求，分 5 个模块：



**模块一：知识库管理（3 项）**

- 创建与管理知识库

- 文档导入（PPT/PDF/DOCX/MD/TXT）

- 文档管理（列表、搜索、删除）



**模块二：知识图谱（4 项）**

- 概念提取（AI 分析文档，增量更新）

- 关系识别（AI 识别概念间知识关联）

- 知识图谱可视化（力导向图，交互操作）

- 节点浮窗 + 文档打开跳转



**模块三：AI 对话助手（4 项）**

- 文档内对话（基于文档内容的 RAG 问答）

- 知识库主页对话（跨文档综合问答）

- 全局对话（学习导航推荐）

- 通用能力（模糊澄清、推荐提问、对话历史）



**模块四：RAG 技术实现（1 项，贯穿模块三）**

- Embedding + 向量数据库 + 检索 pipeline



**模块五：文档阅读器（3 项）**

- 通用文档阅读（PPT 截图、PDF 分页、MD 渲染）

- 文档内搜索

- 从图谱跳转到具体位置



另外 1 个跨系统需求：

- 文档解析管线（多格式兼容解析 + 文本分块 + 向量化入库）



---



## 明确不做（首版边界）



- PPT 讲解模式（模块拆分、步骤展开、高亮——后续版本再加）

- 联网搜索

- 多用户/协作编辑

- 移动端适配

- 学习进度追踪

- 笔记功能



---



## 与课程内容的对应关系（课程报告素材）



| 课程内容                      | 项目中的实践                                      |
| ----------------------------- | ------------------------------------------------- |
| 神经网络与 CNN 原理           | （间接，了解 AI 底层即可）                        |
| PyTorch 环境配置              | （间接，了解 Python 开发环境）                    |
| 分词与词向量（模块三）        | Embedding 向量的生成与语义检索                    |
| Transformer 到 GPT3（模块三） | 调用 LLM API 做概念提取、关系识别、内容生成       |
| Qwen 本地推理（模块三）       | 可选：本地部署 Qwen 替代 API 调用                 |
| LLaMA.cpp（模块三）           | 可选：本地推理加速                                |
| RAG 原理（模块四）            | 核心功能：文档分块 → Embedding → 检索 → 增强生成  |
| LangChain（模块四）           | 可用 LangChain 编排 RAG 流程                      |
| 知识问答助手搭建（模块四）    | AI 对话助手就是知识问答助手                       |
| Agent 概念（模块四）          | 可选扩展：AI 自动判断用户意图（文档内/图谱/全局） |



可以看到，你的项目直接覆盖了模块三和模块四的绝大多数知识点。对于课程报告来说，你有充足内容可以写技术方案、实现细节和效果分析。



---



你觉得这个功能完善方案如何？有没有需要调整的地方？

