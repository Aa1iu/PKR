# 个人知识库管理系统（PK Repository）课程报告

> 课程：人工智能导论 / 大语言模型与 RAG 应用  
> 小组成员：YAL（后端/AI 核心）、dashuai-yan（前端/可视化）、Aaiiu（数据工程/联调）  
> 项目周期：2026.07.19 — 2026.08.20

---

## 项目简要介绍

本项目实现了一个基于 **RAG（检索增强生成）** 架构的个人知识库管理系统：用户上传 PDF/DOCX/PPTX/MD/TXT 文档后，系统自动完成解析、分块、向量化入库，并利用大语言模型自动构建**知识图谱**（概念提取 + 关系识别），最终通过带来源标注的流式对话界面提供智能问答。项目亮点包括：三条完整的 Prompt 工程链路（知识库问答/文档导师/全局导航）、LLM 驱动的图谱构建与增量去重、DeepSeek 风格对话界面与多轮定位交互、以及覆盖五种文档格式的解析渲染管线（含 LibreOffice 幻灯片图片还原）。系统后端基于 FastAPI + ChromaDB + BGE-M3 本地 Embedding，前端基于 React + Ant Design + ECharts/D3，前后端 API 契约先行开发，全程 Mock 驱动并行。

## 成员工作描述及组内量化贡献比

- **YAL 负责**：FastAPI 后端骨架搭建、SQLAlchemy ORM 数据模型设计、Pydantic API 契约定义、DeepSeek LLM 服务封装与 SSE 流式对话框架。
- **dashuai-yan（XZY）负责**：前端全部界面（知识库管理、文档管理、知识图谱可视化、DeepSeek 风格对话面板、浮动对话窗口）、Zustand 状态管理、ECharts 力导向图谱交互、文档阅读器与 Markdown/DOCX/PPT 渲染。
- **Aaiiu（LSC）负责**：ChromaDB 向量库集成、BGE-M3 Embedding 部署、RAG 检索管线（检索-去重-拼接-生成）、LLM 图谱分析（概念提取/去重/关系识别）、文档解析与自动入库流水线、对话历史持久化、PPT 图片渲染，以及全项目 30+ 项缺陷修复与前后端联调。

**YAL : dashuai-yan : Aaiiu = 2 : 3 : 5**

---

# 一、项目背景与需求分析

## 1.1 背景

在大语言模型（LLM）迅速普及的背景下，知识管理的核心痛点从"存储"转向"检索与理解"：用户拥有大量零散文档（课件、笔记、论文），却难以快速定位知识、串联概念、进行跨文档问答。RAG（Retrieval-Augmented Generation）技术通过"先检索后生成"缓解了 LLM 的幻觉问题与知识时效性问题，成为构建个人知识库问答系统的关键技术路径。

本课程项目以"个人知识库管理系统"为题，完整实践了课程模块三（分词与词向量、Transformer、LLM 调用）与模块四（RAG 原理、LangChain、知识问答助手）的核心知识点。

## 1.2 需求概述

| 模块 | 功能需求 |
|---|---|
| 知识库管理 | 多知识库创建/编辑/删除/导出、五种格式文档导入、文档列表/搜索/重命名 |
| 知识图谱 | LLM 自动概念提取、关系识别、力导向图谱可视化、节点浮窗、图谱→文档定位跳转 |
| AI 对话 | 三种场景（文档内/知识库/全局）RAG 问答、SSE 流式输出、来源标注、推荐追问、对话历史持久化 |
| 文档阅读 | PDF 内嵌预览、DOCX 排版还原、PPT 幻灯片图片、Markdown 渲染、全文搜索 |

## 1.3 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | FastAPI + SQLAlchemy + Pydantic | 自动 Swagger 文档、异步 SSE 流式、数据校验 |
| 关系库 | SQLite | 单用户零配置，答辩演示免部署 |
| 向量库 | ChromaDB | 支持 `where kb_id` 元数据过滤，天然按知识库隔离检索 |
| Embedding | BGE-M3（本地） | 中文效果领先、零 API 成本；GPU 加速 |
| LLM | DeepSeek API | 中文能力强、成本极低；预留 Qwen 本地切换 |
| RAG 编排 | 自建管线（参考 LangChain） | 课程模块四理论-实践对应，管线可控可讲 |
| 前端 | React 18 + Vite + Ant Design 5 + ECharts | 组件丰富、图谱快速出图 |

---

# 二、系统架构设计

## 2.1 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  前端（SPA）：React + AntD + ECharts                     │
│  知识库管理 | 文档管理 | 图谱可视化 | 对话面板 | 阅读器    │
└──────────────────────────┬──────────────────────────────┘
                           │ REST + SSE
┌──────────────────────────┴──────────────────────────────┐
│  后端（FastAPI，23 个 API 端点）                          │
│  ├─ 知识库/文档管理（CRUD + 上传 + 全文搜索 + 导出）       │
│  ├─ RAG 管线：检索 → Jaccard 去重 → Prompt 拼接 → 生成   │
│  ├─ 图谱分析：LLM 概念提取 → 向量去重 → 关系识别          │
│  └─ 对话：SSE 流式（token/source/done/error 事件）        │
├──────────────────────────────────────────────────────────┤
│  SQLite（元数据/文档页/概念/关系/对话历史）                │
│  ChromaDB（向量块，按 kb_id 隔离）                        │
│  BGE-M3 本地 Embedding（1024 维） + DeepSeek LLM          │
│  文件存储（uploads/）+ PPT 图片缓存（ppt_cache/）          │
└──────────────────────────────────────────────────────────┘
```

## 2.2 数据流设计

**文档入库链路**：
```
上传 → 保存文件 + 创建记录(processing)
  → 后台任务：逐页解析 → DocumentPage（阅读用）
  → 全文分块（500字/50重叠）→ BGE-M3 向量化 → ChromaDB（RAG 用）
  → status=ready → 自动触发图谱分析
```

**RAG 问答链路**：
```
用户提问 → 查询向量化 → ChromaDB 检索（where kb_id）→ Jaccard 去重
  → Prompt 模板拼接（含来源标注）→ DeepSeek 流式
  → SSE 事件：source → token → done（含推荐追问）
  → 对话历史持久化（ChatMessage 表）
```

**图谱构建链路**：
```
读取全部文档文本 → 重叠分块 → LLM 概念提取（JSON 结构化输出）
  → BGE-M3 向量相似度去重（>0.85 合并）→ 写入 Concept 表
  → LLM 关系识别（5 种关系类型）→ 写入 Relation 表
  → 图谱 API 返回 nodes/edges → 前端力导向图渲染
```

---

# 三、RAG 管线实现（核心）

## 3.1 文档解析与分块

五种格式采用专用库逐格式解析（PyMuPDF/python-pptx/python-docx/chardet），并实现**双通道存储**：

- **DocumentPage 表**：保留真实页面边界（PDF 逐页、PPT 逐幻灯片），供文档阅读
- **ChromaDB 向量块**：全文重新分块（`RecursiveCharacterTextSplitter`，500 字/50 重叠），供 RAG 检索

这一设计的核心洞察：**阅读需要真实页码，检索需要语义完整块**——两者必须分离存储。

## 3.2 向量检索与去重

```python
# ChromaDB 按知识库隔离检索
collection.query(
    query_embeddings=[query_vector],
    n_results=top_k,
    where={"kb_id": kb_id},
)
```

检索结果执行**词级 Jaccard 去重**（中文 2-gram + 英文分词）：相似度 >0.8 的重复段落只保留最高分，避免同一知识点的碎片化结果挤占 Top-K。

## 3.3 Prompt 工程（三种场景）

| 场景 | Prompt 角色 | 上下文注入 | 特色指令 |
|---|---|---|---|
| 知识库问答 | 知识库智能助手 | 检索到的参考资料 + 来源标注 | 引用格式 `[来源: 《文档》P页]` |
| 文档导师 | 文档导师 | 当前文档名 + 页码 + 就近内容 | 优先当前文档，不足时引导看其他文档 |
| 全局导航 | 学习导航助手 | 全部知识库列表（名称+描述） | 推荐学习路径、引导进入具体知识库 |

公共指令：**模糊问题澄清**——问题模糊时先简述理解再回答，信息不足时礼貌追问关键细节。

## 3.4 流式对话（SSE）

四类事件严格对齐前端契约：

```json
data: {"type":"source","sources":[{doc_name, doc_id, page, chunk_text, score}]}
data: {"type":"token","content":"逐字输出"}
data: {"type":"done","message_id":"msg_xxx","follow_up_questions":["追问1","追问2"]}
data: {"type":"error","content":"错误描述"}
```

回答完成后额外调用一次 LLM 生成 2 个**推荐追问**（可点击继续提问），形成对话闭环。

---

# 四、LLM 多场景应用

## 4.1 知识图谱自动构建

**概念提取**：将知识库全部文档的重叠分块拼入 Prompt，要求 LLM 以 JSON 数组返回概念（名称/定义/类型）。使用 `response_format=json_object` 强制结构化输出，并做 Pydantic 校验与重试。

**增量去重**：新概念名称经 BGE-M3 向量化，与库内已有概念计算余弦相似度（>0.85 视为同一概念合并），既避免重复节点，又保证多次分析可增量累积。

**关系识别**：对概念列表二次调用 LLM，识别五种关系（前置依赖/概念延伸/对比关系/包含关系/应用关系），写入 Relation 表并做重复边查重。

**质量保障**：多次分析不产生重复节点/边（实测 3 次分析后概念表 0 重复）；删除文档时同步清理其独占概念与关联边（多文档共享概念保留）。

## 4.2 对话历史与上下文

- 对话历史持久化至 `ChatMessage` 表（重启不丢失）
- 最近 6 轮历史注入 LLM 上下文，形成多轮对话能力
- 全局对话感知全部知识库（名称+描述），提供学习路径推荐

## 4.3 降级与容错

- LLM 调用失败 → SSE `error` 事件（不再伪装成回答文本）
- Embedding 离线模式（`HF_HUB_OFFLINE`），模型缓存后零网络依赖
- SQLite WAL 模式 + 锁等待超时，解决后台任务并发写冲突
- 扫描版 PDF（无文本层）明确标记解析失败并提示原因

---

# 五、前端实现

## 5.1 页面体系

| 页面 | 功能 |
|---|---|
| 首页 | DeepSeek 风格对话面板 + 右侧知识库/文档侧边栏 + 多轮快速定位条 |
| 知识库管理 | 列表/创建/编辑/删除（二次确认）、文档计数 |
| 文档管理 | 拖拽上传、表格列表、全文搜索、重命名、删除确认 |
| 图谱页面 | 知识库选择器、力导向图（着色/定大小/高亮/缩放/拖拽）、"重新分析"按钮 + 轮询 |
| 文档阅读 | PDF iframe、DOCX mammoth、PPT 图片画廊、Markdown 渲染、分页导航 |
| 404 | 兜底页面 |

## 5.2 DeepSeek 风格对话界面

- 空状态：大标题 + 可点击示例问题卡片
- 消息布局：AI 图标 + Markdown 渲染 + 来源引用；用户消息右对齐
- 底部大圆角输入框 + 内部圆形发送按钮 + "AI 可能会出错"提示
- 多轮对话 ≥2 轮时右侧出现**快速定位条**：圆点 hover 显示问题摘要、点击平滑滚动定位并闪烁高亮
- 独立滚动容器（`overscroll-behavior: contain`），页面/侧边栏固定

## 5.3 状态管理与缓存

- Zustand 全局状态：kbStore（知识库/文档缓存，避免重复请求）、chatStore（对话持久化 localStorage）、graphStore、themeStore
- `currentKbId` localStorage 持久化——刷新页面后恢复上次选中的知识库上下文

---

# 六、效果与评估

## 6.1 功能验证

| 功能 | 实测结果 |
|---|---|
| 文档入库 | 17 页 PPTX 解析 → 17 页阅读文本 + 向量块入库（ready） |
| RAG 问答 | 提问"什么是 Dropout" → 检索命中相关文档 + 流式回答 + 来源标注 |
| 知识图谱 | 计算机组成原理库：28 概念节点 / 63 条关系（自动构建） |
| 图谱增量 | 新增 docx 后重新分析：17→22 节点（去重合并生效） |
| PPT 图片 | LibreOffice 转 PNG：首次 8 秒 / 缓存秒开，完整还原幻灯片视觉 |
| 全文搜索 | SQL LIKE 命中返回 snippet，点击跳转定位页码 |

## 6.2 工程化亮点

1. **API 契约先行**：Phase 0 定稿 23 端点，三人并行开发，前后端字段零返工
2. **双通道存储设计**：阅读页与检索块分离，兼顾体验与质量
3. **自动化流水线**：上传→解析→向量化→图谱分析全自动，无需人工干预
4. **30+ 项缺陷修复**：覆盖数据一致性（图谱孤儿节点）、并发安全（SQLite 锁）、边界条件（分页/空值）、体验细节（刷新恢复/选中反馈）等
5. **降级策略完备**：Embedding 离线、PPT 文本降级、扫描 PDF 明确报错

## 6.3 已知限制

- 扫描版 PDF（无文本层）无法提取内容（需 OCR）
- PPT 图片渲染依赖 LibreOffice 本地安装
- 概念-文档关联基于关键词匹配，复杂语义场景可能不精确

---

# 七、总结与展望

本项目完整落地了 RAG 驱动的个人知识库系统，从文档解析、向量化、检索增强生成到 LLM 图谱构建，打通了"输入知识 → 组织知识 → 问答知识"的完整链路。在开发过程中，我们实践了 API 契约先行、Mock 驱动并行、双通道存储、增量去重等工程方法，并通过系统化缺陷排查保证了交付质量。展望后续，可引入 OCR 支持扫描文档、使用更细粒度的语义分块提升检索精度、接入本地 Qwen 模型实现完全离线部署，以及扩展多用户协作与知识推荐能力。

---

# 附录：API 端点清单（23 个）

| 分类 | 端点 |
|---|---|
| 知识库 | GET/POST `/api/kbs`、PUT/DELETE `/api/kbs/{id}`、GET `/export`、GET `/search`、POST `/reindex` |
| 文档 | POST `/docs/upload`、GET `/docs`、GET/PUT/DELETE `/docs/{id}`、GET `/docs/{id}/content`、GET `/docs/{id}/page-image`、GET `/docs/{id}/file` |
| 图谱 | GET `/graph`、GET `/concepts/{id}/positions`、POST `/analyze`、GET `/analyze/status` |
| 对话 | POST `/api/chat`（SSE）、GET/DELETE `/api/kbs/{id}/chat/history`、GET `/api/chat/history` |
| 检索 | POST `/api/search` |
