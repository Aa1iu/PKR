# 环境搭建指南

> **适用环境**：Windows 11 | RTX 5060 (CUDA 12.8) | Miniconda | 2026-07-20

---

## 一、已有环境确认

| 项目 | 当前状态 | 备注 |
|---|---|---|
| Python | 3.13.11 (base) / **3.10.15 (DeepLearning env)** | ✅ 用 DeepLearning env 或新建 |
| Node.js | v24.13.0 | ✅ 版本满足 Vite 需求 |
| npm | 11.6.2 | ✅ |
| GPU | RTX 5060, CUDA 12.8 | ✅ `torch.cuda.is_available()` = True |
| PyTorch | 2.12.0.dev+cu128 (DeepLearning env) | ✅ CUDA 已通 |
| Conda envs | DeepLearning, MinerU, lagcn, ppt-backend, voice_translate, virtual_canteen | — |

---

## 二、创建项目专用 Conda 环境（二选一）

### 方案 A：创建全新环境（推荐 — 干净隔离）

```bash
# 创建 Python 3.11 环境（ML 生态兼容性最好的版本）
conda create -n pk_repo python=3.11 -y

# 激活环境
source /d/GlobalEnv/MiniConda3/etc/profile.d/conda.sh
conda activate pk_repo

# 安装 PyTorch（CUDA 12.8 驱动兼容 cu124 编译版）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# 验证 GPU
python -c "import torch; print('CUDA:', torch.cuda.is_available())"
```

### 方案 B：复用现有 DeepLearning 环境（省时间）

```bash
# DeepLearning 环境已有 Python 3.10.15 + PyTorch 2.12.0.dev+cu128 + CUDA 可用
# 直接安装额外依赖即可，跳过方案 A 的 PyTorch 安装
source /d/GlobalEnv/MiniConda3/etc/profile.d/conda.sh
conda activate DeepLearning
```

> **选择建议**：如果 DeepLearning 环境已有其他项目在用，选方案 A 避免依赖冲突。如果只是个人学习环境，方案 B 更省事。

---

## 三、后端依赖安装

> 以下命令在所选 conda 环境（`pk_repo` 或 `DeepLearning`）中执行。

### 3.1 Web 框架 + 数据库

```bash
pip install fastapi uvicorn[standard] sqlalchemy pydantic python-multipart aiofiles
```

| 包 | 用途 |
|---|---|
| fastapi | Web 框架 |
| uvicorn | ASGI 服务器 |
| sqlalchemy | ORM (SQLite) |
| pydantic | 请求/响应数据校验 |
| python-multipart | 文件上传解析 |
| aiofiles | 异步文件读写 |

### 3.2 向量数据库

```bash
pip install chromadb
```

> ChromaDB 自带轻量级 SQLite 存元数据，无需额外安装。

### 3.3 Embedding 模型（BGE-M3）

```bash
pip install FlagEmbedding
```

验证安装：

```bash
python -c "from FlagEmbedding import BGEM3FlagModel; print('BGE-M3 OK')"
```

### 3.4 LLM API（DeepSeek）

```bash
pip install openai
```

> DeepSeek API 兼容 OpenAI SDK 调用方式。

### 3.5 文档解析库

```bash
pip install PyMuPDF python-pptx python-docx chardet
```

| 包 | 用途 |
|---|---|
| PyMuPDF | PDF 解析 |
| python-pptx | PPTX 解析 |
| python-docx | DOCX 解析 |
| chardet | 文本编码检测（TXT/MD） |

### 3.6 LangChain（轻量使用）

```bash
pip install langchain langchain-community
```

> 仅用 `text_splitter` 和 `vectorstores`，不用 Chain/Agent。

### 3.7 一键安装全部（可选）

```bash
pip install fastapi "uvicorn[standard]" sqlalchemy pydantic python-multipart aiofiles \
    chromadb FlagEmbedding openai PyMuPDF python-pptx python-docx chardet \
    langchain langchain-community
```

---

## 四、前端环境搭建

### 4.1 创建 Vite + React + TypeScript 项目

```bash
cd D:/Project/PK_Repository
npm create vite@latest frontend -- --template react-ts
```

### 4.2 安装依赖

```bash
cd frontend

# UI + 状态管理 + 可视化 + 路由 + HTTP
npm install antd @ant-design/icons zustand echarts echarts-for-react \
    react-router-dom react-markdown remark-gfm axios

# 开发依赖
npm install -D @types/node
```

| 包 | 用途 |
|---|---|
| antd + @ant-design/icons | UI 组件库 |
| zustand | 轻量级状态管理 |
| echarts + echarts-for-react | 知识图谱力导向图 |
| react-router-dom | 前端路由 |
| react-markdown + remark-gfm | Markdown 渲染（AI 回复） |
| axios | HTTP 请求 |

### 4.3 验证前端启动

```bash
npm run dev
```

浏览器打开 `http://localhost:5173`，看到 Vite + React 默认页面即成功。

---

## 五、全局一键安装脚本

将以下内容保存为 `setup_all.sh` 并在 Git Bash 中执行：

```bash
#!/bin/bash
echo "=== PK Repository 环境搭建 ==="

# ---- Conda 环境 ----
echo "[1/4] 创建 Conda 环境..."
source /d/GlobalEnv/MiniConda3/etc/profile.d/conda.sh
conda create -n pk_repo python=3.11 -y
conda activate pk_repo

# ---- PyTorch ----
echo "[2/4] 安装 PyTorch (CUDA 12.4)..."
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# ---- 后端依赖 ----
echo "[3/4] 安装后端依赖..."
pip install fastapi "uvicorn[standard]" sqlalchemy pydantic python-multipart aiofiles \
    chromadb FlagEmbedding openai PyMuPDF python-pptx python-docx chardet \
    langchain langchain-community

# ---- 前端 ----
echo "[4/4] 初始化前端项目..."
cd D:/Project/PK_Repository
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
npm install antd @ant-design/icons zustand echarts echarts-for-react \
    react-router-dom react-markdown remark-gfm axios
npm install -D @types/node

echo "=== 环境搭建完成 ==="
echo "后端验证: cd backend && uvicorn app.main:app --reload --port 8000"
echo "前端验证: cd frontend && npm run dev"
```

---

## 六、环境验证清单

每条命令都应无报错通过：

| 序号 | 验证项 | 命令 |
|---|---|---|
| 1 | Python 版本 | `python --version` → 3.10.x 或 3.11.x |
| 2 | GPU 可用 | `python -c "import torch; assert torch.cuda.is_available()"` |
| 3 | FastAPI 可导入 | `python -c "from fastapi import FastAPI; print('OK')"` |
| 4 | ChromaDB 可导入 | `python -c "import chromadb; print('OK')"` |
| 5 | BGE-M3 可导入 | `python -c "from FlagEmbedding import BGEM3FlagModel; print('OK')"` |
| 6 | OpenAI SDK 可导入 | `python -c "from openai import OpenAI; print('OK')"` |
| 7 | 文档库可导入 | `python -c "import fitz; print('PyMuPDF OK')"` |
| 8 | 后端启动 | `uvicorn app.main:app --reload --port 8000` → `http://localhost:8000/docs` 可访问 |
| 9 | 前端启动 | `npm run dev` → `http://localhost:5173` 可访问 |
| 10 | Node 版本 | `node -v` → v18+ ✅（当前 v24.13.0） |

---

## 七、目录结构（环境搭好后创建）

```
PK_Repository/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/          # config.py, database.py
│   │   ├── models/        # SQLAlchemy ORM 模型
│   │   ├── schemas/       # Pydantic 请求/响应模型
│   │   ├── routers/       # API 路由
│   │   └── services/      # LLM, RAG, Embedding 封装
│   ├── data/              # SQLite, ChromaDB, 上传文件 (gitignore)
│   └── requirements.txt
├── frontend/
│   └── src/
│       ├── api/           # HTTP 请求封装
│       ├── stores/        # Zustand 状态
│       ├── pages/         # 页面组件
│       ├── components/    # 通用组件 (chat, graph, upload, doc-reader)
│       └── types/         # TypeScript 类型
├── mock/                  # Mock JSON 数据
├── scripts/               # ingest.py, search.py
└── projectInfo/           # 团队日志
    ├── YAL/
    ├── XZY/
    └── LSC/
```

---

## 八、常见问题

### Q1: `conda activate` 报错 "Run 'conda init'"

Git Bash 中需要先 source conda 配置：

```bash
source /d/GlobalEnv/MiniConda3/etc/profile.d/conda.sh
conda activate pk_repo
```

### Q2: torch.cuda.is_available() 返回 False

检查 CUDA 驱动版本和 PyTorch 编译版本是否匹配：
```bash
nvidia-smi  # 查看驱动 CUDA 版本
python -c "import torch; print(torch.version.cuda)"  # 查看 PyTorch 编译 CUDA 版本
```
PyTorch cu124 在 CUDA 12.8 驱动上应该正常工作（驱动向后兼容）。

### Q3: ChromaDB 安装失败

ChromaDB 依赖 `onnxruntime`，Windows 上建议：
```bash
pip install onnxruntime  # 先单独装
pip install chromadb
```

### Q4: python-pptx 解析中文乱码

确认安装 `chardet` 并在解析前检测编码，不要直接用系统默认编码读文件。

---

> **下一步**：环境搭好后，按 `项目分工与阶段计划.md` 中 Phase 0 的 7/20 任务推进：
> - A：FastAPI 骨架 + Pydantic Schema
> - B：React 脚手架 + 路由 + Zustand 架子
> - C：Mock JSON 数据
