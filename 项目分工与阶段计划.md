# 项目分工与阶段划分计划（融合终版）

> **项目名称**：个人知识库管理系统（PK Repository）  
> **总工期**：2026.07.19 — 2026.08.20（共 32 天，约 4.5 周）  
> **团队规模**：3 人  
> **核心原则**：API 契约先行、Mock 驱动并行、降级策略带硬 deadline、每个阶段只有一个可验证目标  

---

## 一、角色定义（全栈垂直切分）

```
┌──────────────────────────────────────────────────────────────────┐
│  队员 A：后端核心 & AI 工程师（代码核心）                          │
│  FastAPI · ChromaDB · LLM API · Prompt 工程 · SSE 流式           │
│  关键接口：/upload  /chat  /graph  /search                       │
├──────────────────────────────────────────────────────────────────┤
│  队员 B：前端 & 可视化工程师（门面担当）                          │
│  React · Ant Design · D3.js 图谱 · 文档阅读器 · AI 对话 UI       │
│  关键页面：知识库详情页（图谱）· 文档阅读页 · AI 浮动对话窗口      │
│  关键技巧：Phase 1-2 全程用 Mock 数据，不等后端                     │
├──────────────────────────────────────────────────────────────────┤
│  队员 C：数据工程 & 全栈联调（万金油兜底）                         │
│  文档解析（PyMuPDF/pptx/docx）· SQLite Schema · 双写一致性         │
│  Phase 3-4：前后端联调 · 测试 · Docker/部署文档 · 演示数据准备     │
└──────────────────────────────────────────────────────────────────┘
```

| 代号 | 角色 | 一句话职责 | 对应课程关联 |
|---|---|---|---|
| **A** | 后端/AI 核心 | 所有需要调 LLM、查 ChromaDB、推 SSE 的接口，都是你写 | 模块三 LLM API + 模块四 RAG 管线 |
| **B** | 前端/可视化 | 所有用户能看到、能拖拽、能点击的界面，都是你画 | （前端非课程重点，但图谱是项目亮点） |
| **C** | 数据工程/联调 | 文档→文本→分块→入库的全流程 + A 和 B 中间的胶水层 | 模块三 分词与词向量 + 模块四 LangChain |

---

## 二、按模块分工（含技术栈明细）

### 模块一：知识库管理

| # | 子任务 | 负责人 | 涉及技术 | 工时 |
|---|---|---|---|---|
| 1.1 | 知识库 CRUD（创建/编辑/删除/列表） | **A** API + **B** 页面 | FastAPI + SQLAlchemy；React + AntD Form/Table/Card | 6h |
| 1.2 | 文档导入（拖拽上传 + 批量选择） | **B** 组件 + **A** 接收 | AntD Upload/Dragger；FastAPI UploadFile | 4h |
| 1.3 | 文档解析管线（PPTX/PDF/DOCX/MD/TXT） | **C** | PyMuPDF / python-pptx / python-docx / chardet | 12h |
| 1.4 | 文档分块（差异化策略） | **C** | 自建分块函数（PPT按页聚合 / PDF按段落 / MD按标题 / TXT固定窗口） | 6h |
| 1.5 | 文档管理（列表/搜索/删除/重命名） | **B** 前端 + **A** API | AntD Table/Search；FastAPI 查询接口 | 4h |
| 1.6 | 知识库导出（JSON） | **A** 接口 + **B** 按钮 | FastAPI JSONResponse | 2h |

> 模块一合计：**34h**（A 10h / B 12h / C 12h）

### 模块二：知识图谱

| # | 子任务 | 负责人 | 涉及技术 | 工时 |
|---|---|---|---|---|
| 2.1 | LLM 概念提取（Prompt 设计 + 增量更新 + 去重合并） | **A** | DeepSeek API / Qwen；Pydantic 结构化校验；向量相似度去重 | 10h |
| 2.2 | LLM 关系识别（Prompt 设计 + 方向性 + 简要说明） | **A** | DeepSeek API；关系类型枚举校验 | 8h |
| 2.3 | 图谱数据 API（节点 + 边端点 + 概念-文档位置映射） | **A** | FastAPI REST 接口 | 3h |
| 2.4 | D3.js 力导向图渲染（节点 + 边 + 力模拟） | **B** | D3.js v7（d3-force + SVG）；Mock 数据先行 | 16h |
| 2.5 | 图谱交互（拖拽/缩放/节点高亮/悬停浮窗） | **B** | D3.js zoom/drag；AntD Popover | 10h |
| 2.6 | 浮窗 → 文档跳转（自动滚动定位） | **B** 前端 + **C** 联调 | React Router + scrollIntoView；位置数据 API 对接 | 4h |
| 2.7 | 异步分析触发（文档入库后自动启动概念提取+关系识别） | **C** | FastAPI BackgroundTasks / Celery 轻量替代 | 4h |

> 模块二合计：**55h**（A 21h / B 30h / C 4h）  
> ⚠️ **D3 降级决策点**：8 月 5 日前 B 的力导向图如果节点颜色/大小/高亮三项中任意一项未完成，**立即切 ECharts 关系图**（3 行配置出图，1-2 天替换完成）。

### 模块三：AI 对话助手

| # | 子任务 | 负责人 | 涉及技术 | 工时 |
|---|---|---|---|---|
| 3.1 | AI 对话浮动窗口（全局 Portal + 拖拽/最小化） | **B** | React Portal + AntD Drawer/FloatButton；Zustand | 6h |
| 3.2 | 流式输出 SSE（后端推送 + 前端逐字渲染） | **A** SSE 端点 + **B** EventSource 接收 | FastAPI StreamingResponse；EventSource API | 5h |
| 3.3 | 文档内对话（RAG 上下文注入 + 来源标注） | **A** Prompt + RAG + **B** 上下文传递 UI | Prompt Engineering + ChromaDB 检索 + DeepSeek API | 5h |
| 3.4 | 知识库主页对话（跨文档 RAG + 概念列表上下文） | **A** | Prompt Engineering + ChromaDB 全库检索 | 4h |
| 3.5 | 全局导航对话（轻量 Prompt + 知识库推荐） | **A** 接口 + **B** 首页入口 | DeepSeek API（轻量）；React | 3h |
| 3.6 | 推荐追问生成 | **A** | Prompt Engineering（回答末尾追加 1-2 个问题） | 2h |
| 3.7 | 对话历史管理（按知识库隔离） | **B** localStorage + **C** 后端存储兜底 | localStorage；SQLite 持久化（可选） | 4h |
| 3.8 | 模糊问题澄清 | **A** | Prompt Engineering | 2h |

> 模块三合计：**31h**（A 16h / B 15h / C 0h）

### 模块四：RAG 技术实现（贯穿模块三）

| # | 子任务 | 负责人 | 涉及技术 | 工时 |
|---|---|---|---|---|
| 4.1 | BGE-M3 Embedding 服务部署与调用封装 | **A** | FlagEmbedding (BGE-M3)；本地 CPU 推理 | 6h |
| 4.2 | ChromaDB 集成（Collection 管理 + kb_id 元数据过滤） | **A** | ChromaDB Python API | 5h |
| 4.3 | 文档分块 → 向量化 → 入库流水线 | **A** 索引 + **C** 分块对接 | 分块函数 + BGE-M3 + ChromaDB | 5h |
| 4.4 | 检索接口（top-K + Jaccard 去重 + 来源标注） | **A** | ChromaDB query + 文本相似度去重 | 5h |
| 4.5 | Prompt 模板管理（文档内/知识库级/全局 三种） | **A** | Python 字符串模板 / Jinja2 | 3h |
| 4.6 | 文档删除时向量同步删除 | **A** | ChromaDB delete（where 过滤） | 2h |

> 模块四合计：**26h**（A 21h / C 5h / B 0h）

### 模块五：文档阅读器

| # | 子任务 | 负责人 | 涉及技术 | 工时 |
|---|---|---|---|---|
| 5.1 | Markdown 渲染 | **B** | react-markdown + remark-gfm | 3h |
| 5.2 | PDF 分页文本渲染 | **B** 渲染 + **C** 文本提取 API | pdf.js 或后端 PyMuPDF 提取后分页展示 | 6h |
| 5.3 | PPTX 截图展示 | **C** LibreOffice 转 PNG + **B** 图片画廊 | LibreOffice CLI `--convert-to png` | 5h |
| 5.4 | 纯文本 / DOCX 渲染 | **B** | `<pre>` 或解析后渲染 | 2h |
| 5.5 | 文档内搜索（Ctrl+F 高亮 + 翻跳） | **B** | 纯前端文本匹配 + Mark.js 高亮库 | 4h |
| 5.6 | 图谱跳转自动定位 | **B** 滚动 + **C** 位置数据对接 | scrollIntoView + 段落/页码映射 | 4h |

> 模块五合计：**24h**（B 19h / C 5h / A 0h）  
> ⚠️ **PPT/Word 降级策略**：如果 python-pptx 解析乱序，直接放弃完美排版，只提取纯文本。答辩时说"本版本支持内容提取，格式优化在下一迭代"。

### 跨系统：工程与交付

| # | 子任务 | 负责人 | 涉及技术 | 工时 |
|---|---|---|---|---|
| 6.1 | 项目脚手架（前端 Vite + 后端 FastAPI） | **A** 后端 + **B** 前端 | Vite + FastAPI 初始化 | 4h |
| 6.2 | **API 契约（Swagger JSON 定义）** ⚠️ 最高优先级 | **A** 起草 + **三人** 评审确认 | FastAPI Schema + Swagger UI 自动生成 | 4h |
| 6.3 | Mock 数据准备（供 B 独立开发） | **C** | 静态 JSON 文件（知识库列表/文档列表/图谱 nodes+links/对话消息） | 4h |
| 6.4 | 数据库 Schema 设计（SQLite + ChromaDB 双写一致） | **A** SQLite + **C** ChromaDB | SQLAlchemy ORM + ChromaDB collection 设计 | 4h |
| 6.5 | 前后端联调（Mock → 真实接口切换） | **C**（主导）+ **A** + **B** | — | 8h |
| 6.6 | 本地 Qwen 部署（答辩加分项） | **A** | Qwen-2.5-7B + LLaMA.cpp 量化部署 | 8h |
| 6.7 | 演示数据准备（10 篇 AI 相关文档做测试集） | **C** | 收集/编写覆盖多种格式的测试文档 | 4h |
| 6.8 | 课程报告撰写 | **三人** 各写自己负责模块 | — | 12h |
| 6.9 | 答辩 PPT + 演示视频录制 | **B** PPT 排版 + **三人** 内容 | — | 8h |

> 跨系统合计：**56h**（A 16h / B 18h / C 22h）

---

## 三、工时汇总

| 模块 | 总工时 | A（后端/AI） | B（前端/可视化） | C（数据/联调） |
|---|---|---|---|---|
| 模块一：知识库管理 | 34h | 10h | 12h | 12h |
| 模块二：知识图谱 | 55h | 21h | 30h | 4h |
| 模块三：AI 对话助手 | 31h | 16h | 15h | — |
| 模块四：RAG 技术实现 | 26h | 21h | — | 5h |
| 模块五：文档阅读器 | 24h | — | 19h | 5h |
| 跨系统 | 56h | 16h | 18h | 22h |
| **总计** | **226h** | **84h** | **94h** | **48h** |

### 每日投入估算（3 人组）

| 场景 | 人均日投入 | 32 天可完成 | 需求 226h | 余量 |
|---|---|---|---|---|
| 每天都能投入 | 2.5h | 240h | 226h | 14h（6%） |
| 每天都能投入 | 3h | 288h | 226h | 62h（**27% 缓冲**） |
| 仅工作日（~23 天） | 3.5h | 241h | 226h | 15h（7%） |
| 仅工作日（~23 天） | 4h | 276h | 226h | 50h（22%） |

> 💡 结论：3 个人**平均每天投入 3 小时**就够，周末可以减压；平时课多时每天 2-3 小时，周末补到 4-5 小时即可轻松完成。

---

## 四、阶段划分计划（4 周冲刺）

```
7/19 ─── 7/20 ──── 7/27 ──── 8/03 ──── 8/10 ──── 8/17 ──── 8/19  8/20
  │ 契约  │ 数据层打通 │ RAG链路 │ 图谱冲刺 │ 联调美化 │ 报告   │ 提交
  │ 2天   │   7天     │  7天   │  7天    │  7天    │  2天  │ 截止
  └───────┘           └────────┘         └────────┘         └──────┘
   API定稿          可验证里程碑         可验证里程碑        可验证里程碑
```

---

### Phase 0：API 契约与脚手架（7/19 — 7/20，2 天）

> 🎯 **唯一目标**：前后端 Hello World 联通 + JSON 接口格式定稿

| 日期 | A（后端/AI） | B（前端/可视化） | C（数据/联调） |
|---|---|---|---|
| **7.19** | FastAPI 初始化 + SQLite 建表骨架 | Vite + React + AntD 初始化 | 文档解析技术调研 + 安装 PyMuPDF/pptx/docx |
| **7.20** | **起草全部 API 接口的 Swagger Schema（Pydantic 模型）** | 路由骨架 + Zustand 状态管理架子 | **参与 A 的 Schema 评审 + 准备 Mock JSON 数据** |

**7.20 日必须定稿的 API 契约（JSON 格式）：**

```yaml
# 三人坐下来把以下结构定死，之后 B 就可以完全独立开发

知识库：
  GET  /api/kbs              → {kbs: [{id, name, description, tags, doc_count, created_at}]}
  POST /api/kbs              → 入参 {name, description, tags} → 出参 {id, ...}
  DELETE /api/kbs/{kb_id}    → {success: bool}

文档：
  POST /api/kbs/{kb_id}/docs/upload  → 入参 FormData(file) → 出参 {doc_id, filename, type, pages, size, status}
  GET  /api/kbs/{kb_id}/docs         → {docs: [{doc_id, filename, type, pages, size, status, created_at}]}
  DELETE /api/kbs/{kb_id}/docs/{id}  → {success: bool}
  GET  /api/kbs/{kb_id}/docs/{id}/content?page=1  → {pages: [{page_num, text}], total_pages}

图谱：
  GET /api/kbs/{kb_id}/graph          → {nodes: [{id, name, definition, type, degree, doc_refs: [{doc_id, filename, page}]}], edges: [{source, target, relation, description}]}

对话：
  POST /api/kbs/{kb_id}/chat          → 入参 {question, context_type: "doc"|"kb"|"global", doc_id?: string, page?: int} → SSE 流式 text/event-stream

检索：
  POST /api/search                     → 入参 {kb_id, query} → {results: [{chunk_text, doc_name, page, score}]}
```

> ⚠️ **里程碑 0**（7/20 晚）：Swagger 页面可访问，所有 JSON 结构三人确认签字。**这是后续 B 独立开发的通行证。**

---

### Phase 1：数据层攻坚（7/21 — 7/27，7 天）

> 🎯 **唯一目标**：能用 Python 脚本把一份 PDF 存入 ChromaDB，且能检索出来

| 日期 | A（后端/AI） | B（前端/可视化） | C（数据/联调） |
|---|---|---|---|
| **7.21** | 知识库 CRUD API 全部完成 | 知识库列表页 + 创建/编辑弹窗（**用 Mock 数据**） | PDF 解析器（PyMuPDF）：提取文本 + 页码 |
| **7.22** | 文档上传接收 API + 文件存储 | 文档上传组件（AntD Upload + 拖拽，**先写死回调**） | TXT 解析器 + 编码检测（chardet） |
| **7.23** | 文档删除/列表 API | 文档列表页 + 文档卡片（**Mock 数据渲染**） | DOCX 解析器（python-docx）|
| **7.24** | ChromaDB 集成：创建 collection + 写入测试向量 | 文档阅读页骨架（根据类型切换渲染器，**Mock 内容**） | PPTX 解析器（python-pptx） |
| **7.25-26（周末）** | **BGE-M3 部署 + Embedding 封装函数** | Markdown 渲染组件（react-markdown） | **分块函数**（PPT 按页 / PDF 按段落 / MD 按标题 / 其余固定窗口） |
| **7.27** | **跑通端到端脚本**：PDF → C的分块 → A的Embedding → ChromaDB → A的检索查询 | PDF 分页文本渲染组件 | 分块输出对接 A 的 Embedding 接口 |

> ⚠️ **达成标准**：在终端执行 `python scripts/ingest.py sample.pdf`，然后执行 `python scripts/search.py "卷积神经网络"`，能看到带来源标注的检索结果。**不要求有前端界面。**

---

### Phase 2：RAG 核心链路（7/28 — 8/3，7 天）

> 🎯 **唯一目标**：在网页上传一份 PDF，能在聊天框里基于文档内容回答问题

| 日期 | A（后端/AI） | B（前端/可视化） | C（数据/联调） |
|---|---|---|---|
| **7.28** | RAG 检索 API（`POST /api/search`） | 前端对接真实上传接口（**第一个脱离 Mock 的接口**） | 文件元信息存入 SQLite + ChromaDB collection 按 kb_id 创建 |
| **7.29** | **Prompt 模板**：文档内场景 v1 + LLM 调用封装（DeepSeek SDK） | AI 浮动对话窗口组件（Portal + 打开/关闭/最小化 + 位置拖拽，**先写死回复**） | 检索引擎压力测试（确保分块-Embedding-入库-检索四步无误） |
| **7.30** | **SSE 流式端点**（`POST /api/kbs/{kb_id}/chat`）→ 对接 DeepSeek | SSE EventSource 接收 + 逐字渲染 | 分块策略调优（验证不同格式的检索命中率） |
| **7.31** | Prompt 模板：知识库级场景 + RAG 全库检索 | 对话 UI 完整实现（消息列表 + Markdown 渲染回复 + 输入框 + 发送按钮） | B 接口联调：文档上传全流程走通 |
| **8.1-2（周末）** | Prompt 模板：全局导航场景 + 推荐追问 + 模糊澄清 | 对话历史（localStorage 按 kb_id 隔离）+ 来源引用高亮 | **演示文档集准备**（收集 10 篇 AI/深度学习相关 PDF/MD/TXT） |
| **8.3** | **三个场景对话全链路打通**（QA + 真实检索 + SSE） | 三场景对话入口对接（文档页/图谱页/首页的对话按钮） | 全流程联调：上传文档 → 等向量化 → 对话提问 → SSE 出结果 |

> ⚠️ **达成标准**：8/3 晚，在网页上传一份 PDF → 等 30 秒入库 → 打开文档阅读页 → 点 AI 对话框 → 输入"这篇文档主要讲了什么？" → 看到流式输出带来源引用（`[来源: 《XXX》P3]`）的回答。**这是 RAG 闭环跑通的标志，课程报告的核心素材诞生。**

---

### Phase 3：知识图谱视觉冲刺（8/4 — 8/10，7 天）

> 🎯 **唯一目标**：D3.js 图谱页面能显示至少 10 个节点和 15 条边，点击节点能看详情

| 日期 | A（后端/AI） | B（前端/可视化） | C（数据/联调） |
|---|---|---|---|
| **8.4** | **LLM 概念提取**：Prompt 设计 + 调 DeepSeek 输出结构化概念 JSON + Pydantic 校验 | **D3.js 力导向图基础渲染**（节点圆圈 + 文字标签 + 边线条 + d3-force 布局，**用 Mock nodes + edges**） | 把演示文档集全部入库 + 跑完分块+向量化 |
| **8.5** | 概念去重合并（名称相似度 > 0.8 合并）+ 增量更新逻辑 | **⚠️ D3 降级决策点**：节点颜色/大小/高亮任一项未完成 → 停 D3 切 ECharts | 概念提取结果入库（SQLite concepts 表 + relations 表） |
| **8.6** | **LLM 关系识别**：Prompt 设计 + 5 种关系类型枚举 + 方向性 + 说明 | 图谱交互：缩放（滚轮）+ 拖拽节点 + 画布平移 | 关系识别结果入库 + 图谱数据 API 对接测试 |
| **8.7** | 图谱数据 API（`GET /api/kbs/{kb_id}/graph`）→ 返回 nodes + edges JSON | 图谱交互：点击节点 → 高亮直接关联节点和边（其余节点半透明） | 图谱 API ↔ 前端 Mock 数据格式对齐（保证字段名一致） |
| **8.8** | 概念-文档位置映射 API（给定概念名+doc_id → 返回页码列表） | 悬停浮窗（AntD Popover）：概念名 + 定义 + 类型标签 + 关联文档名列表 | Mock → 真实数据切换（B 的图谱接上 A 的真接口） |
| **8.9** | 文档入库后自动触发异步分析（BackgroundTasks） | 浮窗中文档名可点击 → 跳转文档阅读页 + scrollIntoView 定位 | 全链路联调：上传新文档 → 自动抽取概念/关系 → 图谱增量更新 |
| **8.10（缓冲）** | 图谱数据质量检查（概念重复率 / 关系覆盖率 / 异常 JSON 处理） | 图谱加载动画 + 空知识库占位图 + 错误状态处理 | 性能测试：10 篇文档的知识库，图谱节点 50+ 时页面是否卡顿 |

> ⚠️ **达成标准**：8/10 晚，知识库中有 10 篇 AI 文档 → 打开图谱页 → D3（或 ECharts）力导向图展示 30-80 个概念节点 → 节点按类型着色 → 连线表示关系 → 点节点看详情浮窗 → 点浮窗中"关联文档"跳转阅读。

---

### Phase 4：集成联调与答辩准备（8/11 — 8/17，7 天）

> 🎯 **唯一目标**：全流程无阻塞跑通，Qwen 本地部署可演示，PPT 大纲完成

| 日期 | A（后端/AI） | B（前端/可视化） | C（数据/联调） |
|---|---|---|---|
| **8.11** | **Qwen-2.5-7B 本地部署**（LLaMA.cpp 量化 INT4）+ 环境踩坑 | UI 统一走查：间距/配色/字体/空状态/加载态/错误提示/404 页面 | **端到端回归测试**：按答辩演示流程完整走 3 遍，记录所有 bug |
| **8.12** | 替换一个 LLM 调用点为本地 Qwen（建议：概念提取），对比 API 结果 | 前端打包优化 + 路由守卫 + 页面标题 | Bug 分派：C 判断 bug 归属（A 的后端还是 B 的前端）→ 分发修复 |
| **8.13** | Qwen 不可用时的自动降级开关（环境变量切换） | 全流程联调：图谱→文档→对话→来源跳转 + 修复链路断点 | Docker 封装（Python 后端 + ChromaDB 持久化）+ 本地启动脚本 |
| **8.14** | Swagger metadata 完善（description/tags/example）+ 接口文档截图 | 用户操作引导（首次进入知识库时 AntD Tour 提示） | 用户手册/部署文档（README.md：怎么启动、怎么导入文档、怎么对话） |
| **8.15** | Prompt 最后一轮调优（检索结果质量验证 + 回答准确性抽查） | 演示流程排练 + UI 最后一轮微调 | 准备答辩演示数据：精选 10 篇文档，确保图谱节点丰富、对话效果好看 |
| **8.16-17（周末）** | **课程报告**：写 RAG 管线章节 + LLM 应用章节 | **课程报告**：写系统设计章节 + 图谱可视化章节 + **答辩 PPT** | **课程报告**：写文档解析章节 + 数据工程章节 + 部署章节 |

> ⚠️ **达成标准**：8/17 晚，功能冻结，代码提交到 GitHub，报告初稿完成，PPT 大纲定稿。**不再加新功能。**

---

### Phase 5：收尾交付（8/18 — 8/19，2 天）

> 🎯 **唯一目标**：报告定稿 + PPT 完工 + 演示视频录制 + 提交就绪

| 日期 | 三人共同 |
|---|---|
| **8.18** | **课程报告终稿**（整合三人章节，统一文风，补充架构图 + 截图 + 效果数据）+ 代码仓库清整（删调试代码、加 README、确保 `git clone` 后可运行） |
| **8.19** | **演示视频录制**（走核心流程：首页 → 创建知识库 → 上传 3 份文档 → 查看知识图谱 → 点击节点 → 跳转文档 → 打开 AI 对话提问 → 展示来源引用 → 全局对话导航）+ **答辩 PPT 定稿** + 三人预演一遍 |

> 🚀 **8.20（四）**：提交。课程报告 + 代码仓库 + PPT + 演示视频。

---

## 五、甘特图总览

```text
                 7/19  7/21  7/27        8/3         8/10        8/17     8/19 8/20
                    │     │     │           │           │           │        │    │
Phase 0  契约+脚手架 ██    │     │           │           │           │        │    │
Phase 1  数据层攻坚  ░░░░░███████           │           │           │        │    │
Phase 2  RAG核心链路 ░░░░░░░░░░░███████████ │           │           │        │    │
Phase 3  图谱冲刺    ░░░░░░░░░░░░░░░░░░░░░░░███████████│           │        │    │
Phase 4  联调+答辩   ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░███████████│        │    │
Phase 5  收尾交付    ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░██████████    │
                    │     │     │           │           │           │        │    │
                  M0     M1    M2          M3          M4          M5     提交 截止
                契约定稿 脚本  网页上传     图谱可视     功能冻结   报告终稿
                       跑通   对话跑通     交互完整     Qwen可用   PPT完工
```

---

## 六、硬性降级策略（带截止日决策点）

| 触发日期 | 风险项 | 判定标准 | 降级方案 | 负责人拍板 |
|---|---|---|---|---|
| **8/5** | D3.js 力导向图 | 节点颜色 + 大小 + 高亮，三项中任一项未完成 | **立刻切 ECharts** 关系图（1-2 天替换，3 行配置出图） | **B** |
| **8/3** | PPT/DOCX 解析 | 解析出的文本内容可读性低于 70% | **放弃完美排版**，只提取纯文本；答辩说"格式优化在下一迭代" | **C** |
| **8/6** | LLM 概念提取质量 | 10 篇文档产出概念 < 20 个或重复率 > 50% | **收紧 Prompt**（限定最少 3 个/篇）+ 降低去重阈值到 0.7 | **A** |
| **8/10** | BGE-M3 本地部署失败 | 无法生成 embedding 向量 | **备选：DeepSeek Embedding API**（一行代码切换，费用极低） | **A** |
| **8/12** | Qwen 本地部署失败 | 显存不足（< 6G）或推理 > 30s/token | **只保留接口预留**，答辩时说"架构上已适配本地模型，受限于演示环境 GPU" | **A** |
| **8/15** | 全流程有阻塞性 bug | 答辩演示主流程（上传→图谱→对话→跳转）有一步不可用 | **三人全部扑 bug**，暂停报告撰写 | **C**（协调） |

---

## 七、分工总览（按技术栈归类）

### 🅰️ A — 后端/AI 核心（~84h）

| 技术领域 | 具体技术 | 对应模块 |
|---|---|---|
| Web 框架 | FastAPI + Pydantic + SQLAlchemy | 全模块 API |
| API 文档 | FastAPI 自动 Swagger + ReDoc | Phase 0 |
| 关系数据库 | SQLite（Schema 设计、CRUD） | 模块一 |
| 向量数据库 | ChromaDB（Collection、元数据过滤 `where kb_id`、增删同步） | 模块四 |
| Embedding | BGE-M3 本地（FlagEmbedding）；备选 DeepSeek Embedding API | 模块四 |
| LLM 调用 | DeepSeek API（概念提取 + 关系识别 + 对话生成）；Qwen 本地（答辩） | 模块二、三 |
| RAG 管线 | 检索 → 去重 → Prompt 拼接 → SSE 流式输出 | 模块三、四 |
| Prompt 工程 | 三种场景模板 + 推荐追问 + 模糊澄清 + 来源标注格式 | 模块三 |

### 🅱️ B — 前端/可视化（~94h）

| 技术领域 | 具体技术 | 对应模块 |
|---|---|---|
| 前端框架 | React 18 + Vite + TypeScript | 全模块 UI |
| UI 组件库 | Ant Design 5（Form / Table / Upload / Card / Drawer / FloatButton / Popover / Tour） | 模块一、三 |
| 状态管理 | Zustand（kbStore / chatStore） | 全模块 |
| 图谱可视化 | D3.js v7（d3-force + zoom/drag + SVG）；**降级备选 ECharts** | 模块二 |
| 文档渲染 | react-markdown + pdf.js 或后端文本分页 | 模块五 |
| 流式对话 UI | EventSource SSE 接收 + 逐字渲染 + Markdown 格式化回复 | 模块三 |
| 对话历史 | localStorage（按 kb_id 隔离命名空间） | 模块三 |
| 文档内搜索 | Mark.js 高亮 + 翻跳按钮 | 模块五 |
| 打包构建 | Vite build | 跨系统 |

### 🅲️ C — 数据工程/联调（~48h）

| 技术领域 | 具体技术 | 对应模块 |
|---|---|---|
| 文档解析 | PyMuPDF (PDF) / python-pptx (PPTX) / python-docx (DOCX) / chardet (编码) | 模块一、五 |
| 文本分块 | 自建差异化分块（PPT 按页聚合 / PDF 按段落 / MD 按标题 / 其余固定窗口） | 模块一 |
| PPT→PNG 转换 | LibreOffice CLI `--convert-to png` | 模块五 |
| 数据库 | SQLite 元数据表 + ChromaDB 双写一致性维护 | 模块一、四 |
| Mock 数据 | 静态 JSON（为 B 提供独立开发基础） | Phase 0 |
| 联调测试 | Mock→真实接口切换 + 端到端回归 + Bug 分发 | Phase 3-4 |
| 演示数据 | 10 篇 AI 主题多格式测试文档集 | Phase 4 |
| 部署文档 | Docker + 本地启动脚本 + README.md | Phase 4 |
| 课程报告 | 文档解析 + 数据工程 + 部署 + 效果评估章节 | Phase 5 |

---

## 八、Mock 数据清单（Phase 0 产出，C 负责，B 据此独立开发）

```json
// 1. 知识库列表 Mock（文件：mock/kbs.json）
{ "kbs": [
  { "id": "kb_001", "name": "深度学习基础", "description": "吴恩达课程笔记", "tags": ["深度学习", "入门"], "doc_count": 8, "created_at": "2026-07-20" },
  { "id": "kb_002", "name": "计算机组成原理", "description": "期末复习资料", "tags": ["计组"], "doc_count": 5, "created_at": "2026-07-21" }
]}

// 2. 文档列表 Mock（文件：mock/kb_001_docs.json）
{ "docs": [
  { "doc_id": "doc_001", "filename": "CNN入门.pdf", "type": "pdf", "pages": 15, "size": "2.3MB", "status": "analyzed", "created_at": "2026-07-22" },
  { "doc_id": "doc_002", "filename": "反向传播推导.pptx", "type": "pptx", "pages": 20, "size": "5.1MB", "status": "processing", "created_at": "2026-07-22" }
]}

// 3. 图谱 Mock（文件：mock/kb_001_graph.json）
{ "nodes": [
  { "id": "n1", "name": "卷积神经网络", "definition": "一种包含卷积计算的前馈神经网络", "type": "基础概念", "degree": 8, "doc_refs": [{"doc_id": "doc_001", "filename": "CNN入门.pdf", "page": 3}] },
  { "id": "n2", "name": "卷积核", "definition": "在输入数据上滑动的小型权重矩阵", "type": "技术方法", "degree": 5, "doc_refs": [{"doc_id": "doc_001", "filename": "CNN入门.pdf", "page": 5}] },
  { "id": "n3", "name": "池化层", "definition": "对特征图进行下采样的网络层", "type": "技术方法", "degree": 4, "doc_refs": [{"doc_id": "doc_001", "filename": "CNN入门.pdf", "page": 8}] },
  { "id": "n4", "name": "图像分类", "definition": "将图像归入预定义类别的任务", "type": "应用场景", "degree": 3, "doc_refs": [{"doc_id": "doc_001", "filename": "CNN入门.pdf", "page": 1}] }
],
  "edges": [
    { "source": "n1", "target": "n2", "relation": "包含关系", "description": "卷积核是CNN的核心计算单元" },
    { "source": "n1", "target": "n3", "relation": "包含关系", "description": "池化层是CNN的典型组件" },
    { "source": "n1", "target": "n4", "relation": "应用关系", "description": "CNN广泛应用于图像分类" }
  ]
}

// 4. 对话消息 Mock（文件：mock/chat_messages.json）
{ "messages": [
  { "role": "user", "content": "什么是卷积核？", "timestamp": "2026-07-22T10:00:00" },
  { "role": "assistant", "content": "卷积核（Convolution Kernel）是一个在输入数据上滑动的小型权重矩阵。根据《CNN入门》第5页的描述，卷积核通过在输入特征图上滑动并进行逐元素乘法求和，实现对局部特征的提取 [来源: 《CNN入门》P5]。", "timestamp": "2026-07-22T10:00:03" }
]}
```

---

## 九、课程报告章节建议（边开发边积累）

| 章节 | 内容 | 哪位写 | 素材来源 |
|---|---|---|---|
| 1. 项目背景与需求 | 知识管理的痛点 + RAG 技术简介 + 课程内容关联 | 三人合写 | Requirements.md |
| 2. 系统架构设计 | 整体架构图 + 技术栈选型理由 + API 契约设计 | **B** | 技术栈选择.md + Phase 0 产物 |
| 3. RAG 管线实现 | 分块策略 + BGE-M3 Embedding + ChromaDB + 检索→增强→生成全流程 | **A** | 模块四开发过程 |
| 4. LLM 多场景应用 | 概念提取 + 关系识别 + 三种对话场景的 Prompt 工程 | **A** | 模块二、三开发过程 |
| 5. 知识图谱构建与可视化 | 图谱数据流 + D3.js 力导向图实现 + 交互设计 | **B** | 模块二开发过程 |
| 6. 文档解析与数据工程 | 多格式兼容解析 + 差异化分块 + 双写一致性 | **C** | 模块一、五开发过程 |
| 7. 本地模型部署 | Qwen-7B 量化部署过程 + API vs 本地对比分析 | **A** | Phase 4 |
| 8. 效果评估与总结 | RAG 有无对比 + 检索准确率 + 项目收获与展望 | **C** | 测试截图 + 全组总结 |

---

## 十、每日站会检查项（5 分钟，每晚）

```
1. 今天完成了什么？（对应本文档哪个子任务编号？）
2. 有什么阻塞？（需要 A/B/C 哪位配合？）
3. 明天做什么？（是否赶上阶段里程碑？）
```

> 📋 推荐工具：飞书多维表格 / GitHub Projects（三列：Todo / In Progress / Done，每个卡片对应一个子任务编号）

---

> 📅 **关键日期速查**  
> 07.20 契约定稿 | 07.27 脚本检索跑通 | 08.03 网页对话跑通 | 08.05 D3 降级决策 | 08.10 图谱可视完成 | 08.14 功能冻结 | 08.17 报告初稿 | 08.19 PPT+视频 | **08.20 提交**
