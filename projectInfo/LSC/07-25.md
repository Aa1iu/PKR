# 07-25 执行日志 — LSC (C)

---

## 【14:00】执行修改 — Phase 1 后端 ChromaDB 集成 + 脚本补完 + API 对接

### 执行指令草稿

按 plan 文件 `crystalline-roaming-clover.md`，按以下顺序实现：

1. 创建 `backend/app/services/chroma_service.py`：ChromaDB 管理类，提供 collection 管理 + 向量 CRUD
2. 创建 `backend/app/services/document_parser.py`：将 ingest.py 中的解析/分块函数提取为共享模块
3. 补完 `scripts/ingest.py` 第 3 步：对接 EmbeddingService + ChromaService + SQLite 双写
4. 补完 `scripts/search.py`：对接 EmbeddingService + ChromaService + Jaccard 去重
5. 对接 API 端点：`/api/search` 真实检索、`/api/kbs/{kb_id}/search` 全文搜索、文档上传后台解析流水线、文档删除同步 ChromaDB

### 草稿自检结果

- 逻辑完整：ChromаDB 读写 → 脚本 → API，链路完整
- 与历史一致：Phase 0 API 契约未修改，所有变更在已有骨架内补完
- 贴合目标：M1 标准（脚本检索跑通）依赖的 ChromaDB 链路已贯通
- 无语病

### 用户确认记录

- ChromaDB Collection 策略：确认选 A（每 KB 一个 Collection）
- SQLite 双写：确认需要写入
- 页码追踪：确认选 B（粗略估算，Phase 2 再精确）
- 环境：pk_repo conda 环境

### 实际执行内容

#### 新建文件（3 个）

| 文件 | 说明 |
|---|---|
| `backend/app/services/chroma_service.py` | ChromaDB 管理服务：get_collection、add_chunks、search、delete_by_doc_id、delete_by_kb_id、count。每 KB 一个 collection (`kb_{kb_id}`) |
| `backend/app/services/document_parser.py` | 共享文档解析模块：parse_document(5 种格式)、chunk_text(LangChain)、estimate_page_num |
| `projectInfo/LSC/07-25.md` | 本日志 |

#### 修改文件（6 个）

| 文件 | 变更内容 |
|---|---|
| `backend/app/services/__init__.py` | 添加所有服务导出（ChromaService、EmbeddingService、LLMService、RAGService、document_parser） |
| `scripts/ingest.py` | 步骤 3→4 重编：Embedding 生成 → ChromaDB 写入 → SQLite DocumentPage 双写 → 更新 Document.status。改用共享 document_parser 模块 |
| `scripts/search.py` | 实现完整检索链：Embedding → ChromaDB search → Jaccard 去重 → 格式化输出 |
| `backend/app/routers/search.py` | `POST /api/search` 对接 EmbeddingService + ChromaService + Jaccard 去重，返回真实结果 |
| `backend/app/routers/kbs.py` | `GET /api/kbs/{kb_id}/search` 全文搜索：SQL LIKE 查询 DocumentPage.text，返回 snippet |
| `backend/app/routers/docs.py` | upload 端点添加 BackgroundTasks 后台解析流水线；delete 端点添加 ChromaDB 向量同步删除 |

#### 功能验证结果

```text
ChromaDB 写入 ✅  count=1
ChromaDB 检索 ✅  1 result, score=1.0
ChromaDB 删除 ✅  count=0 after delete
所有 .py 文件语法检查 ✅ (8/8 通过)
服务导入链路 ✅
```

---

---

## 【16:00】执行修改 — Code Review 发现 9 个 Bug 修复

### 修复清单

| # | 文件 | 修复内容 | 严重度 |
|---|---|---|---|
| 1 | `scripts/ingest.py:102` | 页码估算参数传错：`estimate_page_num(i, chars_per_page_est)` → `estimate_page_num(i, args.chunk_size, chars_per_page_est)` | 高 |
| 2 | `routers/kbs.py:delete_kb` | KB 删除时新增 `chroma_service.delete_by_kb_id(kb_id)` 同步清理 ChromaDB 向量 | 高 |
| 3 | `routers/search.py` + `scripts/search.py` | Jaccard 去重从字符级 `set(text)` 改为词级 2-gram + 空格分词；提取到共享模块 `document_parser.py`，消除两处重复定义 | 中 |
| 4 | `routers/search.py` | 移除未使用的 `db: Session = Depends(get_db)` 参数 | 中 |
| 5 | `scripts/ingest.py:85-87` | chunks 为空时 early return 改为输出完成消息后退出 | 中 |
| 6 | `routers/kbs.py:search_kb` | 全文搜索 q 参数加 `.strip()` 防止空白查询 | 低 |
| 7 | `routers/kbs.py:search_kb` | `page_size` 加 `min(page_size, 100)` 上限 | 低 |
| 8 | `routers/docs.py` | `SessionLocal` 本地 import 移到文件顶部 | 低 |
| 9 | `routers/chroma_service.py:search` | 空 collection 或 `top_k<=0` 时提前 `return []`，避免 `n_results=0` crash | 低 |

### 验证结果

- 所有 7 个 Python 文件语法检查 ✅
- `ingest.py` 端到端 ✅：2 chunks → 1024-dim → ChromaDB + SQLite → status=ready
- `search.py` 检索"CNN"✅：词级 Jaccard 去重 3→2 条，返回带来源标注结果
- 不影响 A 的代码（chat.py / graph.py / models / config / database 未改动）

---

## 下一步（7/26-27）

1. 【高】BGE-M3 模型网络问题解决后，关掉 `MOCK_EMBEDDING` 验证真实 Embedding
2. 【高】启动 FastAPI 后端，用 Swagger `/docs` 测试所有端点
3. 【中】转告 A 修复 ChatRequest schema validator + models 已知问题
4. 【低】更新 `requirements.txt` 添加 `langchain-text-splitters`（如已安装可跳过）
