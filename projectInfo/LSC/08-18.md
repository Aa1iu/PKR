# 08-18 执行日志 — LSC (C)

---

## 【14:00】里程碑检查 — 成员 C 交付物全面盘点

### 执行内容

对照分工计划中 C 的全部任务，逐项盘点仓库现状：

| # | 交付物 | 状态 |
|---|---|---|
| 1.3 | 文档解析管线（PDF/DOCX/PPTX/MD/TXT） | ✅ `document_parser.py` 五格式齐全 |
| 1.4 | 差异化分块（500字/50重叠 + Jaccard 去重） | ✅ 同文件 |
| 6.3 | Mock 数据（4 份 JSON） | ✅ 格式对齐契约（图谱 8 节点/8 边） |
| 6.4 | ChromaDB collection 设计 | ✅ `chroma_service.py` |
| 6.5 | 前后端联调 | ✅ 历史日志记录（8/12、8/16 多轮） |
| 6.7 | 演示数据 | ✅ uploads 35 文件（md 22 / pdf 11 / docx 1 / pptx 1），DB：2 库 12 文档 263 页 45 概念 78 关系 37 对话 |
| 6.8 | 课程报告 C 章节（第 6、8 章） | ✅ `课程报告.md` |
| 8.14 | README 用户手册/部署文档 | ❌ **缺失（仅一行标题）→ 本次补齐** |
| 8.13 | Docker 封装 + 本地启动脚本 | ❌ **完全缺失 → 本次补齐** |
| — | 核心链路实测验证 | ❌ 未做过独立冒烟测试 → 本次补齐 |

### 备注

盘点结论：C 的核心代码交付物均已就绪，缺口集中在**部署文档与工程化收尾**。

---

## 【14:30】执行修改 — README 用户手册重写（任务 8.14）

### 执行内容

README.md 从仅一行标题重写为完整用户手册，共 11 节：核心功能与实测效果、技术栈、目录结构、环境要求、快速开始（后端/前端启动步骤）、使用指南（创建知识库→导入文档→对话→图谱四步流）、CLI 脚本、API 概览（23 端点）、环境变量配置表、Docker 部署、FAQ（7 条常见问题与降级方案）、团队分工。

### 修正记录

- 启动命令实测校准：入口为 `uvicorn app.main:app`（旧文档写作 `main:app`，已按真实目录结构修正）
- 前端实际版本校准：React 19 / AntD 6 / Vite 8（计划文档写 React 18 / AntD 5，按 package.json 实况填写）

---

## 【15:00】执行修改 — Docker 封装 + 启动脚本（任务 8.13）

### 执行内容

| 文件 | 说明 |
|---|---|
| `backend/Dockerfile` | python:3.11-slim，容器内 CPU 推理，HF_HOME 可挂载复用模型缓存 |
| `docker-compose.yml` | 端口 8000、backend/data 持久化卷、HF_CACHE_DIR 模型缓存挂载、DEEPSEEK_API_KEY 环境变量注入 |
| `backend/.dockerignore` | 排除 data/ 与 __pycache__ |
| `scripts/setup_backend.bat` | Windows 一键建 conda 环境 + 装依赖 + 导入自检 |
| `scripts/start_backend.bat` | 启动后端：Key 缺失时交互询问、无 GPU 自动降级 CPU |
| `scripts/start_backend.sh` | 同上的 Git Bash / Linux 版本（团队 Git Bash 环境可用） |
| `scripts/start_frontend.bat` / `.sh` | 前端启动：首次自动 npm install |

### 备注

- start_backend.bat 使用延迟展开读取输入 Key，已开启 `EnableDelayedExpansion`
- 降级策略落实：无 GPU 时自动 `EMBEDDING_DEVICE=cpu`，与降级计划表一致

---

## 【15:30】执行修改 — 文档管线冒烟测试脚本 + 实测（端到端验证）

### 执行内容

新增 `scripts/verify_pipeline.py`：对 uploads 下每种格式各抽 1 个文件，验证"全文解析→逐页解析→重叠分块→Jaccard 去重"全链路，并检测扫描版 PDF（逐页无文字时告警）。

**技术要点**：用 importlib 按文件路径直接加载 `document_parser.py`，绕过 `services/__init__.py` 的包级导入——避免冒烟测试被迫安装 chromadb / FlagEmbedding 等重型依赖。

### 实测结果（17 项全部通过）

| 格式 | 全文解析 | 逐页解析 | 分块 |
|---|---|---|---|
| PDF (13 页) | 13212 字符 | 13/13 页有文字 | 35 块（均值 401 字） |
| MD | 69 字符 | 1 页 | 1 块 |
| PPTX (12 页) | 1292 字符 | 12/12 页 | 4 块（均值 331 字） |
| DOCX | 15224 字符 | 1 页 | 46 块（均值 326 字） |
| Jaccard 去重 | 3 条高相似结果 → 去重后 2 条（保留高分）✅ | | |

### 踩坑记录

1. Windows 控制台 GBK 编码无法打印 "✓" 字符 → 测试输出改用纯 ASCII，脚本 exit code 0
2. `fitz` API 弃用警告（新版 PyMuPDF 建议 `import pymupdf`）→ 现有代码仍可用，留给下一迭代处理

---

## 【16:00】阶段里程碑检查

对照 Phase 4-5 达成标准：

- ✅ 功能冻结：全流程代码已提交仓库
- ✅ README 用户手册与部署文档：本次补齐
- ✅ Docker 封装 + 本地启动脚本：本次补齐
- ✅ 端到端验证：文档管线冒烟测试 17/17 通过（全链路含 Embedding/ChromaDB 的验证需 GPU 机器跑 ingest.py，留待演示前执行）
- ⏳ 报告终稿整合 + 演示视频：按计划 8/19 与 A、B 共同完成

**风险项**：无阻塞性风险。演示前建议在完整环境（GPU + DeepSeek Key）跑一次 `ingest.py` → `search.py` 全链路确认。
