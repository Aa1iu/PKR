# 项目技术栈选型分析

以下从**前端、可视化、后端、数据库、AI/RAG、文档解析、部署**七个维度，逐一分析候选技术的优劣和适用环境。

## 最终技术栈选择
- 前端框架：React 18+
- UI组件：Ant design
- 知识图谱：ECharts（后期可改用D3.js）
- 后端框架：FastAPI
- 数据库：SQLite
- 向量数据库：ChromaDB
- Embedding模型：BGE-m3
- RAG：LangChain


---

## 一、前端框架

| 维度                 | **React 18+**                                             | **Vue 3**                        | **Next.js 14+**                |
| -------------------- | --------------------------------------------------------- | -------------------------------- | ------------------------------ |
| **学习曲线**         | 中等（JSX + Hooks 心智模型）                              | 较低（模板语法直观，中文文档好） | 较高（SSR/CSR 区分、路由约定） |
| **生态成熟度**       | ⭐⭐⭐⭐⭐ 最大生态                                       | ⭐⭐⭐⭐ 成熟但规模小一号        | ⭐⭐⭐⭐⭐ 基于 React          |
| **中文社区**         | 好                                                        | **非常好**（尤雨溪、中文官网）   | 好                             |
| **与本项目的匹配度** | 高。复杂交互（图谱拖拽、浮动对话框）用 React 状态管理自然 | 高。适合单页应用                 | 过度设计。本项目不需要 SSR/SEO |
| **部署**             | 纯静态 SPA，Nginx 即可                                    | 同 React                         | 需要 Node 运行时               |

> **推荐：React 18+**（Vite 构建）
>
> 理由：知识图谱可视化（D3.js）和浮动对话窗口这类重交互场景，React 的组件化 + 状态管理更自然。Vite 构建速度极快，开发体验好。如果团队更熟悉 Vue，Vue 3 + Vite 也是很好的选择——差距不大。
>
> **明确不推荐 Next.js**：课程项目不需要 SSR，Next.js 增加不必要的复杂性。

---

## 二、UI 组件库

| 维度           | **Ant Design 5**                        | **shadcn/ui**                    | **Material UI (MUI)**           |
| -------------- | --------------------------------------- | -------------------------------- | ------------------------------- |
| **组件丰富度** | ⭐⭐⭐⭐⭐ 表格、文件上传、树形控件都有 | ⭐⭐⭐ 基础组件为主              | ⭐⭐⭐⭐                        |
| **定制化**     | 主题定制（Design Token），但仍有限      | **完全可控**（组件源码在项目中） | 主题系统成熟                    |
| **包体积**     | 较大（按需加载可缓解）                  | **最小**（无额外依赖）           | 较大                            |
| **中文支持**   | ⭐⭐⭐⭐⭐ 蚂蚁集团出品                 | ⭐⭐⭐ 需自行配置                | ⭐⭐                            |
| **学习成本**   | 中等                                    | 低（本质是 Radix + Tailwind）    | 中等                            |
| **适合场景**   | 需要大量开箱即用组件的后台系统          | 追求 UI 自主可控、有设计追求     | 遵循 Material Design 规范的项目 |

> **推荐：Ant Design 5**
>
> 理由：本项目有大量后台管理类 UI（文件列表、知识库卡片、多级菜单、上传组件），Ant Design 的 Table、Upload、Tree、Card 等组件能大幅减少前端工作量。既然你的课程核心是 RAG 和 LLM，前端 UI 不值得从头造轮子。
>
> 如果对 UI 的"千人一面"不满意，shadcn/ui 是第二选择——但它缺的文件上传、树形选择器等需要自己组装。

---

## 三、知识图谱可视化

这是本项目前端最具技术含量的部分。

| 维度                 | **D3.js v7**                                | **ECharts 5**                | **Cytoscape.js**                   | **Vis.js (vis-network)** |
| -------------------- | ------------------------------------------- | ---------------------------- | ---------------------------------- | ------------------------ |
| **力导向图**         | 原生 `d3-force` 模拟，完全可控              | 内置力导向图，配置简单       | 有但非核心优势                     | 内置，API 简单           |
| **定制化**           | ⭐⭐⭐⭐⭐ 无限灵活                         | ⭐⭐⭐ 受限于 chart 范式     | ⭐⭐⭐⭐ 图论专用                  | ⭐⭐⭐                   |
| **性能（200+节点）** | 好（可手动优化 tick）                       | 好（WebGL 渲染可选）         | **最好**                           | 中等（200+ 开始卡）      |
| **交互能力**         | 需手写（拖拽、缩放、tooltip）               | 内置交互但定制受限           | 内置交互丰富                       | 内置交互基本够用         |
| **学习曲线**         | **陡峭**（需要理解 enter/update/exit 模式） | 平缓（百度开源，中文文档好） | 中等                               | 平缓                     |
| **中文文档**         | 一般（英文为主）                            | **非常好**                   | 一般                               | 一般                     |
| **适合场景**         | 需要完全自定义的交互和样式                  | 快速出图，不追求极致定制     | 图分析（最短路径、聚类等算法需求） | 中小规模图谱快速搭       |

> **推荐：D3.js v7**
>
> 理由：你的需求文档中描述的交互非常具体——节点按类型着色、节点大小反映关联密度、边的线型区分关系类型、点击高亮直接关联节点、悬停浮窗。这些定制化需求用 ECharts 会处处受限（ECharts 的力导向图不支持通过配置区分边的线型），用 D3 虽然代码量更多，但每个细节都可控。
>
> **折中方案**：用 ECharts 快速出第一版（2-3 天），如果定制需求满足不了再切 D3。对于课程答辩，ECharts 出的图已经够好看。如果你的组里有前端较强的同学，一步到位用 D3。

---

## 四、后端框架

| 维度          | **FastAPI**                     | **Flask**            | **Django**                |
| ------------- | ------------------------------- | -------------------- | ------------------------- |
| **异步支持**  | ⭐⭐⭐⭐⭐ 原生 async/await     | ⭐⭐ 需插件          | ⭐⭐⭐ 4.1+ 支持          |
| **API 文档**  | **自动生成 Swagger + ReDoc**    | 需手动 / Flask-RESTX | 需 DRF + drf-spectacular  |
| **文件上传**  | `UploadFile` 原生支持，简洁     | 需手动处理           | 内置 FileField            |
| **WebSocket** | **原生支持**                    | 需 Flask-SocketIO    | 需 Channels               |
| **学习成本**  | 中等（Pydantic 模型是额外学习） | **最低**             | 高                        |
| **适合场景**  | API 服务、前后端分离            | 小型 API、原型       | 全栈应用、需要 Admin 后台 |

> **推荐：FastAPI**
>
> 理由有三：
> 1. **WebSocket 原生支持**——AI 对话如果要做流式输出（SSE 或 WebSocket），FastAPI 写起来最自然
> 2. **自动生成 API 文档**——课程答辩时可以打开 Swagger UI 展示所有接口，效果很好
> 3. **Pydantic 数据校验**——文档解析、LLM 返回的 JSON 都可以用 Pydantic 做结构化校验，代码更健壮
>
> Flask 也可以，但实现流式对话和自动文档需要更多额外工作。Django 太重，不适合本项目。

---

## 五、数据库

### 5.1 关系型数据库（元数据存储）

| 维度           | **SQLite**                | **PostgreSQL**                |
| -------------- | ------------------------- | ----------------------------- |
| **部署复杂度** | ⭐⭐⭐⭐⭐ 零配置，单文件 | ⭐⭐ 需安装服务               |
| **单用户性能** | 好                        | 好                            |
| **并发能力**   | 差（写锁整个库）          | 好（MVCC）                    |
| **向量扩展**   | 无                        | pgvector 插件可替代向量数据库 |
| **适合场景**   | 单用户桌面/个人应用       | 多用户服务端应用              |

> **推荐：SQLite**
>
> 理由：本项目是**单用户个人知识库管理**，没有并发写入需求。SQLite 零配置、免安装，答辩演示时不需要额外启动数据库服务。而且 SQLite + SQLAlchemy 的搭配让未来切换到 PostgreSQL 也很容易（换一行连接字符串即可）。
>
> 如果考虑 pgvector 替代 ChromaDB（减少一个组件），可以选 PostgreSQL。但 pgvector 的检索质量不如专用向量数据库，学习成本也更高。**建议保持 SQLite + ChromaDB 的简单组合**。

### 5.2 向量数据库

| 维度           | **ChromaDB**                     | **FAISS**            | **Milvus**         |
| -------------- | -------------------------------- | -------------------- | ------------------ |
| **部署**       | `pip install` 即可               | `pip install` 即可   | 需 Docker/服务部署 |
| **持久化**     | **内置支持**                     | 需手动实现           | 内置支持           |
| **元数据过滤** | **内置支持**（按知识库 ID 过滤） | 需手动维护映射       | 内置支持           |
| **性能**       | 中等（10万级向量足够）           | **最高**             | 高（百万级优化）   |
| **内存占用**   | 低                               | 中高                 | 高                 |
| **适合场景**   | 中小项目快速集成                 | 百万级以上高性能需求 | 企业级、分布式     |

> **推荐：ChromaDB**
>
> 理由：最适合课程项目规模。"知识库 ID 过滤"这个特性对项目很关键——你需要限定检索范围在某个知识库内，不能跨库检索。ChromaDB 的 `where` 参数直接支持：
>
> ```python
> collection.query(query_embeddings=[...], n_results=5,
>                  where={"kb_id": "kb_001"})
> ```
>
> FAISS 性能更强但需要自己封装持久化和元数据过滤，工作量大且跟课程核心内容无关。Milvus 是企业级方案，对本项目严重过度。

---

## 六、文档解析

| 格式         | 推荐方案                             | 备选               | 说明                                                                              |
| ------------ | ------------------------------------ | ------------------ | --------------------------------------------------------------------------------- |
| **PDF**      | **PyMuPDF (fitz)**                   | pdfplumber         | PyMuPDF 速度最快，中文提取效果好，支持页码级提取                                  |
| **PPTX**     | **python-pptx**                      | LibreOffice 转 PNG | 提取文本用 python-pptx 原生 API；图片渲染用 LibreOffice 命令行 `--convert-to png` |
| **DOCX**     | **python-docx**                      | —                  | 标准库，稳定可靠                                                                  |
| **Markdown** | 直接读文件 + `mistune`/`markdown-it` | —                  | 前端用 `react-markdown` 渲染                                                      |
| **TXT**      | 直接读文件                           | —                  | 注意编码检测（`chardet`）                                                         |

**批量方案 vs 逐个方案：**

像 `Unstructured.io` 这样的库可以一站式处理所有格式，但：
- 依赖重（需要 libreoffice、poppler 等多个系统级依赖）
- 灵活性差（分块策略不可定制）

> **推荐：逐格式专用库**
>
> 理由：5 种格式各自用最成熟的 Python 库，组合起来代码量不大（每种格式一个 parser 函数，100 行以内），但灵活性最高。你的需求文档强调了不同格式的分块策略不同（PPT 按页聚合、PDF 按段落、MD 按标题），专用方案更容易实现差异化分块。

---

## 七、LLM 与 Embedding

### 7.1 大语言模型

| 维度         | **DeepSeek API**             | **Qwen 本地部署**             | **OpenAI API** |
| ------------ | ---------------------------- | ----------------------------- | -------------- |
| **中文能力** | ⭐⭐⭐⭐⭐                   | ⭐⭐⭐⭐                      | ⭐⭐⭐         |
| **费用**     | **极低**（约 ¥1/百万 token） | 免费（但需要 GPU）            | 较高           |
| **部署**     | API 调用，零部署             | **需要 GPU（建议 8G+ 显存）** | API 调用       |
| **特殊优势** | —                            | 展示"本地推理部署"能力        | 生态最好       |
| **适合场景** | 日常开发 + 答辩              | 答辩加分项                    | 通用           |

> **推荐：DeepSeek API 为主，Qwen 本地部署为答辩加分项**
>
> 策略：开发阶段全程使用 DeepSeek API（便宜、稳定、快），答辩前用 1-2 天部署 Qwen-7B 或 Qwen-2.5-7B 本地模型，替换部分 LLM 调用（如概念提取），在答辩时展示"本项目同时支持云端 API 和本地模型部署"。
>
> 课程模块三专门讲了 Qwen 本地推理和 LLaMA.cpp，部署 Qwen 天然是报告素材。

### 7.2 Embedding 模型

| 维度         | **DeepSeek Embedding** | **BGE-M3 (本地)**            | **OpenAI text-embedding-3** |
| ------------ | ---------------------- | ---------------------------- | --------------------------- |
| **中文效果** | 好                     | **最好**（BGE 中文评测领先） | 中等                        |
| **部署**     | API 调用               | `pip install FlagEmbedding`  | API 调用                    |
| **维度**     | 1536                   | 1024（可调）                 | 256/1024/3072               |
| **费用**     | 极低                   | 免费                         | 中等                        |

> **推荐：BGE-M3 本地部署**
>
> 理由：Embedding 不像 LLM 那样对实时性要求高（文档入库时跑一次即可），且 BGE-M3 在消费级 CPU 上也能跑（不用 GPU）。用本地方案既省钱又能在报告中写"基于 BGE-M3 的本地 Embedding 服务"，比"调了 API"有更多技术细节可展开。
>
> 如果需要极简方案，DeepSeek Embedding API 是可靠的备选。

---

## 八、RAG 编排框架

| 维度           | **LangChain**               | **LlamaIndex**      | **自建 Pipeline**      |
| -------------- | --------------------------- | ------------------- | ---------------------- |
| **与课程关联** | ⭐⭐⭐⭐⭐ 课程模块四直接讲 | ⭐⭐⭐ 不是课程内容 | ⭐⭐⭐⭐               |
| **RAG 专用性** | 通用框架，RAG 是子集        | **专为 RAG 设计**   | 完全自主               |
| **代码量**     | 中等                        | 少                  | 多                     |
| **灵活性**     | 中等（LangChain 抽象层多）  | 中等                | **最高**               |
| **学习成本**   | 中等                        | 较低                | 最低（但实现成本最高） |
| **风险**       | 版本迭代快，API 不稳定      | 同左                | 无                     |

> **推荐：LangChain（但只用需要的部分）**
>
> 理由：你的课程模块四讲的就是 LangChain，用它写报告时可以直接引用课程内容，理论-实践对应。但 LangChain 的坑在于它封装太多层，出了问题不好 debug。
>
> 建议的最小化用法：
> ```python
> # 只用到 LangChain 的这几个组件
> from langchain.text_splitter import RecursiveCharacterTextSplitter
> from langchain_community.vectorstores import Chroma
> from langchain_community.embeddings import HuggingFaceBgeEmbeddings
> ```
>
> 不要用 LangChain 的 Agent、Chain 等高级抽象——你的 RAG 流程很简单（检索 → 拼 prompt → 调 LLM），不需要 LangChain 的"链式调用"。
>
> 如果 LangChain 让你感到痛苦，**直接用自建 pipeline 完全可行**：ChromaDB 的 Python API 本身就足够简洁，拼 prompt 就是 f-string，调 LLM 就是 HTTP request。

---

## 九、综合推荐方案

```text
┌─────────────────────────────────────────────────────────┐
│                      前端 (SPA)                          │
│  React 18 + Vite + TypeScript                           │
│  Ant Design 5 (UI)  +  D3.js v7 (图谱)                  │
│  react-markdown (MD渲染)   状态管理: Zustand             │
└──────────────────────┬──────────────────────────────────┘
                      │ REST + SSE (流式对话)
┌──────────────────────┴──────────────────────────────────┐
│                   后端 (Python)                          │
│  FastAPI + SQLAlchemy + Pydantic                        │
│  ChromaDB (向量)  +  SQLite (元数据)                     │
│  PyMuPDF / python-pptx / python-docx (文档解析)          │
│  BGE-M3 本地 / DeepSeek Embedding API (Embedding)       │
│  DeepSeek API / Qwen 本地 (LLM)                         │
└─────────────────────────────────────────────────────────┘
```

| 组件       | 推荐                                | 开发阶段                           | 答辩阶段                    |
| ---------- | ----------------------------------- | ---------------------------------- | --------------------------- |
| 前端框架   | React 18 + Vite                     | ✅ 使用                            | ✅                          |
| UI 库      | Ant Design 5                        | ✅ 使用                            | ✅                          |
| 图谱可视化 | D3.js v7                            | ✅ 使用（或先用 ECharts 快速出图） | ✅                          |
| 后端框架   | FastAPI                             | ✅ 使用                            | ✅                          |
| 关系数据库 | SQLite                              | ✅ 使用                            | ✅                          |
| 向量数据库 | ChromaDB                            | ✅ 使用                            | ✅                          |
| 文档解析   | PyMuPDF + python-pptx + python-docx | ✅ 使用                            | ✅                          |
| LLM        | **DeepSeek API**                    | ✅ 使用                            | ➡️ **切换部分为 Qwen 本地** |
| Embedding  | **BGE-M3 本地**                     | ✅ 使用                            | ✅                          |
| RAG 编排   | 自建 pipeline（参考 LangChain）     | ✅ 使用                            | ✅                          |

---

## 十、关于"本地部署 vs 云端 API"的策略建议

这是课程报告中最需要讨论的话题，也是老师最可能提问的：

| 场景                 | 方案                            | 理由                          |
| -------------------- | ------------------------------- | ----------------------------- |
| **开发调试**         | 全部用 API                      | 快速验证流程，不浪费 GPU 算力 |
| **概念提取（离线）** | 可以用本地 Qwen                 | 非实时，速度不重要，省钱      |
| **AI 对话（在线）**  | 用 API（DeepSeek）              | 用户等待，需要速度            |
| **答辩展示**         | 切换至少一个核心环节到本地 Qwen | 展示"本地模型推理部署"能力    |
| **Embedding**        | 始终本地 BGE-M3                 | 离线任务，本地跑完全够用      |

**答辩话术建议**：
> "本项目的 AI 能力采用混合架构：Embedding 层使用 BGE-M3 本地部署，实现零成本的文档向量化；LLM 层支持热切换——开发阶段使用 DeepSeek API 保证响应速度，同时集成了 Qwen-7B 本地推理作为离线方案，验证了课程模块三所学的模型本地部署技术。"
